# CI Restoration — Issue: lockfile/quality gate outage since 2026-06-21

Session: 2026-06-22

## Summary

The CI pipeline is green locally. All quality-gate commands pass, and the fixes have been committed in small logical units.

## Changes made

1. **Lockfile / dependency reconciliation**
   - Regenerated `pnpm-lock.yaml` with `pnpm install` (non-frozen).
   - Removed unused direct devDependency `@babel/helper-plugin-utils`.
   - Pinned `vitest` back to `^2.1.9` (the previous green baseline).

2. **Lint/type failures from merge**
   - `src/hooks/audioEngine/audioPlayback.ts`: removed stray closing brace left by the drum-retrigger optimization merge.
   - `src/components/NoteSelector.tsx`: added `"vocoderMix"` to `onPropertyChange` key union.
   - `src/hooks/useAppState.tsx`: added `"vocoderMix"` to `handleNotePropertyChange` key union.
   - `src/__tests__/RbsCorpus.test.ts`: guarded against `undefined` summary (RBS test was failing the gate; minimal fix only).

## Vitest 4 vs 2 decision

**Decision:** Pin vitest back to `^2.1.9`.

**Rationale:** Vitest 4 requires Vite 6+ (`vite/module-runner` export). Adopting it would force a Vite 5 → 6 migration, which is a broad build-tool change out of scope for restoring the gate. No source code uses vitest-4-specific APIs.

**Deferred follow-up:** Schedule a deliberate vitest/vite major upgrade, validating `@fast-check/vitest` peer compatibility.

## Final gate status (local)

| Command | Status | Notes |
|---------|--------|-------|
| `pnpm install --frozen-lockfile` | ✅ PASS | Lockfile consistent |
| `pnpm run lint` | ✅ PASS | |
| `pnpm run build:wasm` | ✅ PASS | AssemblyScript + Rust + JC-303 |
| `pnpm run build:emcc` | ✅ PASS | hyphon_native + Rubberband + 40 export mappings |
| `pnpm test` | ✅ PASS | 1129 passed, 1 skipped |
| `pnpm run build` | ✅ PASS | Vite production build, `dist/` generated |
| `pnpm exec playwright test` | ✅ PASS (with skips) | 1 passed, 7 skipped (skipped tests depend on fixture/environment conditions) |

## Commits

- `5a17ef8` — chore(deps): restore vitest ^2.1.9, remove unused @babel/helper-plugin-utils, regenerate lockfile
- `a0998fa` — fix(audioPlayback): remove stray closing brace from drum retrigger merge
- `3b5fbc7` — fix(types): add vocoderMix to per-step property change key unions
- `8b356fc` — test(RbsCorpus): guard undefined summary before building golden summary
- `b38c306` — docs(swarm-state): record CI restoration final state and deferred follow-ups

## Notes / deferred items

- Vitest/vite major upgrade is intentionally deferred.
- Playwright E2E passes locally when `dist/` is served on `http://localhost:5173` via `pnpm run preview --port 5173`; 7 tests are skipped due to fixture/environment preconditions and 1 runs successfully. CI will run the same suite after installing browsers.
- WASM artifacts were rebuilt locally but are not committed because they are regenerated by CI/build scripts.
