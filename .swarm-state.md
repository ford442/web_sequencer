# Swarm State — E2E Playwright matrix (#1036 / #1039)

## Status
**TESTING** — Firefox RBS green ×3 after PulseAudio sink + StartOverlay e2e unlock. Running full firefox → webkit → chromium.

## Projects
| Project   | Status | Notes |
|-----------|--------|-------|
| chromium  | was green | re-verify |
| firefox   | nearly green | RBS needed PulseAudio; without sink AC stays `suspended` |
| webkit    | pending | same PulseAudio dependency likely |

## Specs / root causes addressed
1. WebKit launch: Chromium-only `--autoplay-policy`
2. `webServer` block
3. Rack / selector helpers
4. StartOverlay testids + `?e2e=1` non-blocking `resume()` + gesture unlock
5. `tests/global-setup.ts` starts PulseAudio (Firefox AC otherwise never reaches `running`)
6. firefoxUserPrefs autoplay
7. `.orig` purge

## Changed since last checkpoint
- PulseAudio discovery + globalSetup
- RBS `startPlaybackAndAwaitClock` helper
- StartOverlay capture-phase native resume

## Resume commands
```bash
cd /workspace
git checkout cursor/e2e-playwright-matrix-2ef1
pulseaudio --check || pulseaudio --start --exit-idle-time=-1
pnpm exec playwright test --project=firefox --workers=1 --repeat-each=2
pnpm exec playwright test --project=webkit --workers=1 --repeat-each=2
pnpm exec playwright test --project=chromium --repeat-each=2
pnpm exec tsc -b && pnpm lint
pnpm exec playwright test --repeat-each=2
```
