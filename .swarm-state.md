Iteration 1 — 2026-04-21T16:34:32.696+02:00

What I changed this iteration

- Added runtime telemetry singleton: src/utils/engineTelemetry.ts
- Added lightweight HUD overlay: src/components/EngineHUD.tsx (auto-mounts; toggled by Ctrl+Shift+E or ?hud=1)
- Imported HUD in main.tsx for side-effect mounting
- Instrumented audio initialization in src/hooks/useAudioEngine.ts:
  - Registered backend resolutions for oscillators (webgpu/wasm/wav/js), Open303 (jc303), and singingVoice manager
- Instrumented pyodide readiness in src/hooks/usePyodideEngine.ts
- Instrumented WASM loader bridges to register resolutions:
  - src/utils/fftLoader.ts (FFT)
  - src/utils/trackFreezer.ts (trackFreezer)
  - src/utils/audioExport.ts (audio export)

What works

- Telemetry singleton implemented and exported as `engineTelemetry`
- HUD overlays telemetry snapshot and updates every 500ms; shows backend badges, p50/p95 (if recorded) and error counts
- Initial backend registration added at several init points (oscillators, jc303, singingVoice, pyodide, FFT, trackFreezer, audioExport)

What is still broken / outstanding

- Lint/test could not run in this environment: `eslint: not found` (node_modules not installed). Full validation requires installing dev dependencies.
- Not all subsystems are instrumented yet (TTS Supertonic service, Rust audio init path, xmExport, open303 finer-grained statuses)
- Latency recording (recordLatency) not yet wired into hot-path calls — next iteration will add lightweight timing wrappers at caller boundaries (audioPlayback / voiceManager call sites)
- No unit tests added yet — plan to add a small Vitest test for engineTelemetry.snapshot()

Next planned iteration

1. Add latency recording at caller boundaries (audioPlayback createPlaySynth / createPlayDrum) and sample recordLatency calls for FFT/TTS calls.
2. Add unit test for engineTelemetry
3. Expand instrumentation to TTS/Rust/Open303 bridges where safe to modify
4. Run `npm install` in a CI or developer environment and rerun `npm run lint` and `npm test`; address any lint/type failures introduced by changes

Blockers / notes for Noah

- CI/dev machine needs node_modules installed (npm install) before lint/test/build will run correctly in this environment
- Full `npm run build` may require Emscripten/emsdk and wasm-pack for native WASM pieces; these are out of scope for this swarm iteration

Files touched (summary)

- CREATED: src/utils/engineTelemetry.ts
- CREATED: src/components/EngineHUD.tsx
- UPDATED: src/main.tsx
- UPDATED: src/hooks/useAudioEngine.ts
- UPDATED: src/hooks/usePyodideEngine.ts
- UPDATED: src/utils/fftLoader.ts
- UPDATED: src/utils/trackFreezer.ts
- UPDATED: src/utils/audioExport.ts

If you want me to continue: next actions are wiring latency recordings, adding tests, and instrumenting remaining subsystems. If you'd like, install dependencies and I can re-run lint/tests and iterate until green on touched files.

Iteration 2 — 2026-04-21T15:15:02Z

What I changed this iteration

- Instrumented Rust audio bridge: src/engines/RustOscillator.ts (registerResolution on init success/failure, recordLatency in generate).
- Instrumented JC-303/Open303: src/engines/Open303Oscillator.ts (registerResolution on worklet ready/failure, fallback activation; recordLatency on noteOn/noteOff), src/engines/Open303Manager.ts (registerResolution summarizing backends), and src/hooks/audioEngine/audioPlayback.ts (recordLatency around open303 note dispatch paths).
- Added engineTelemetry imports where needed.

What works

- Rust and JC-303 init paths now call engineTelemetry.registerResolution on success/failure.
- NoteOn/NoteOff and generate callsites now record sample latency into engineTelemetry.

What is still broken / outstanding

- Lint/test still couldn't run here (no package-lock.json / node_modules).
- More hot-paths need instrumentation (voice managers, TTS).
- Add unit test for engineTelemetry.snapshot.

Next planned iteration

1. Run lint & tests after installing dependencies; fix any issues.
2. Add Vitest unit for engineTelemetry.snapshot().
3. Continue instrumenting TTS and Rust call-sites.

Blockers / notes

- package-lock.json missing; npm ci cannot run in this environment.
- Full build may require Emscripten/wasm-pack on CI.

Files touched (additional)

- UPDATED: src/engines/RustOscillator.ts
- UPDATED: src/engines/Open303Oscillator.ts
- UPDATED: src/engines/Open303Manager.ts
- UPDATED: src/hooks/audioEngine/audioPlayback.ts

If you'd like me to continue, run `npm install` (or provide a CI runner with the lockfile) and I'll re-run lint/tests and iterate until the touched files are green.

