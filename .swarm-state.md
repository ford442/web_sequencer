Iteration 1 — 2026-05-19T04:12:00+02:00
Status: build-passing
Files changed: src/components/KnobGPUContext.ts (created), src/components/MagicKnob.tsx (refactored)
What was done: Created `KnobGPUContext.ts` as a module-level singleton that owns one `GPUDevice`, one `GPURenderPipeline`, and one shared `requestAnimationFrame` loop for all holographic knob instances. The WGSL shader was moved verbatim from `MagicKnob.tsx` into the singleton. Each registered knob gets its own `GPUBuffer` (size 32) and `GPUBindGroup`; the pipeline and shader module are shared. Refactored `MagicKnob.tsx` to remove the per-instance WebGPU setup (~190 lines eliminated). It now calls `KnobGPUContext.register(canvas, getValueFn)` on mount and `KnobGPUContext.unregister(handle)` on cleanup. The singleton lazily initializes the GPU on first registration, handles WebGPU unavailability gracefully (returns `null`, canvas stays blank), and stops the RAF loop when the last knob unregisters. TypeScript check (`tsc -b`) passes with zero errors.
What remains: None — acceptance criteria met.
Blockers: none

Iteration 2 — 2026-05-25T15:43:00+02:00
Done
- Eliminated per-component WebGPU init and RAF from `HardwareModule.tsx` (~320 lines removed).
- Replaced single shared canvas with one `<canvas>` per knob, absolutely positioned inside the module container.
- Each knob canvas registers with `KnobGPUContext.register(canvas, getValueFn)` via a stable callback ref on mount, and unregisters on unmount or reorder.
- Added a `ResizeObserver` on the container to size each knob canvas (CSS size + DPR-backed store) and re-render 2D fallbacks on resize.
- Updated `renderWith2D` to draw a single knob with the contracted palette: near-black background (#0d0f13), cyan outer ring (#00e5ff) at ~55% radius, cyan-to-teal gradient arc (#00e5ff → #00897b) over a 270° sweep, and a white needle.
- Interaction event listeners were retargeted from the old single canvas to the module container `div`; hit-detection math is unchanged.
- `KnobGPUContext.ts` was not modified — its existing API surface is sufficient.
Build status
- `npm run build` passes (zero TypeScript errors, Vite production build succeeds).
Remaining
- None — all acceptance criteria met.
Blockers
- None.
