## Checkpoint 2

### Work items
- [x] P0-a: SessionLaunchEngine partition regression test
- [x] P0-b: PcfEffect + masterLoudnessStage `?worker&url` conversion
- [x] P0-b: ESLint guard (addModule raw .ts literal) + `audioWorkletModuleUrls.test.ts`
- [x] P0-b: `check-release-dist.mjs` worklet + raw-.ts assertions
- [ ] P0-c: Export map delivery + diagnostic logging

### Commands last run
- `pnpm exec tsc -b` — PASS
- `pnpm run lint` — PASS
- `CI=true pnpm run test:unit` — PASS (includes audioWorkletModuleUrls)
- `pnpm run build:release` — PASS (native + vite + check-release-dist)
- `node scripts/check-release-dist.mjs` — PASS (6 required worklets in dist)

### Files touched
- `src/engines/PcfEffect.ts` — `?worker&url` import replaces raw `.ts` new URL
- `src/audio/loudness/masterLoudnessStage.ts` — same fix
- `eslint.config.js` — no-restricted-syntax for raw .ts addModule literals
- `src/__tests__/audioWorkletModuleUrls.test.ts` — static guard (new)
- `scripts/check-release-dist.mjs` — required worklet processors + no raw .ts in dist

### Findings
- **masterLoudnessStage verdict: SAME BUG as PcfEffect** — confirmed identical `new URL(...*.ts)` pattern; `createMasterLoudnessStage` returned null on failure, silently killing limiter/LUFS in production. Fixed with `?worker&url`; `dist/assets/master-loudness-processor-Biv8Kq-A.js` now emitted.
- `pcf-processor` chunk not in dist because `PcfEffect` is only type-imported (not instantiated in reachable graph); fix is correct for when wired. `useSustainProcessor.ts` still uses `'sustain-processor.js'` string — out of scope.
- Dist worklet chunks confirmed: clock, open303, prophecy, sustain, rubberband, master-loudness. No raw `.ts` in dist JS.

### Next action
P0-c: verify export map in dist, regenerate if stale, extend check-release-dist, add export-name logging in Open303/Prophecy.
