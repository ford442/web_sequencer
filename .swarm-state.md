# Swarm State — E2E Playwright matrix (#1036 / #1039)

## Status
**IN PROGRESS** — `.orig` purge committed+pushed. Next: harness fixes (webServer, WebKit launch args, shared StartOverlay helper, module selectors).

## Projects
| Project   | Status | Notes |
|-----------|--------|-------|
| chromium  | red    | engine303 + highfid fail: `[class*="rounded"]` no longer matches HardwareModule (chrome panels use `hyphon-rack-surface`) |
| firefox   | red    | same selector failures; RBS import click timeout |
| webkit    | red    | **hard fail**: `--autoplay-policy=no-user-gesture-required` is Chromium-only and kills WebKit launch |

## Specs still flaking / failing (from CI run 30779397045)
- `engine303-switch.spec.ts` — all 5: module not found via `[class*="rounded"]`
- `highfid-engine-matrix.spec.ts` — all 4: same
- `rbs-automation.spec.ts` — firefox click timeout on import; webkit launch fail
- `knob-canvas-smoke.spec.ts` — webkit launch fail only (chromium/firefox passed)

## Changed since last checkpoint
1. Isolated commit `1cab259e`: deleted 5 `.orig` merge artifacts; no code imports them.
2. Branch: `cursor/e2e-playwright-matrix-2ef1` (pushed).

## Root-cause plan (next edits)
1. `playwright.config.ts`: add `webServer`; move autoplay arg to chromium project only.
2. `StartOverlay.tsx`: add `data-testid` hooks (no restyle).
3. `tests/helpers/boot.ts` (shared): deterministic overlay dismiss via testid + `/?e2e=1`.
4. Fix module locators → `.hyphon-rack-surface` / `.hyphon-chrome-panel` with hasText.
5. Unify specs to shared helper; add `test:e2e` script.

## Resume commands
```bash
cd /workspace
git checkout cursor/e2e-playwright-matrix-2ef1
pnpm install --frozen-lockfile
# If WASM missing:
#   source "$HOME/emsdk/emsdk_env.sh" && pnpm run build:wasm && pnpm run build:emcc
pnpm exec playwright install --with-deps
pnpm exec playwright test --project=chromium --repeat-each=3
```
