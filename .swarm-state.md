# Swarm State — E2E Playwright matrix (#1036 / #1039)

## Status
**TESTING** — harness committed+pushed (PR #1046). WASM built locally. Running chromium next.

## Projects
| Project   | Status | Notes |
|-----------|--------|-------|
| chromium  | testing | — |
| firefox   | pending | — |
| webkit    | pending | Chromium-only autoplay arg removed from shared config |

## Specs / root causes addressed
1. WebKit launch: `--autoplay-policy=…` scoped to chromium project only
2. `webServer` block added (reuseExistingServer: true)
3. Rack mounts only `selectedTrack` — helpers `selectRackTrack` / `openRackModule`
4. Module locator: `.hyphon-rack-surface` (not `[class*="rounded"]`)
5. StartOverlay: `data-testid="start-overlay"` / `initialize-system`
6. `.orig` purge (commit 1cab259e)

## Changed since last checkpoint
- PR #1046 opened (draft)
- Commits: orig purge, harness fix, Bass matcher cleanup
- Local: `build:wasm` + `build:emcc` + playwright browsers installed

## Resume commands
```bash
cd /workspace
git checkout cursor/e2e-playwright-matrix-2ef1
source "$HOME/emsdk/emsdk_env.sh"
# WASM already built in this VM; re-run if missing:
#   pnpm run build:wasm && pnpm run build:emcc
pnpm exec playwright test --project=chromium --repeat-each=3
pnpm exec playwright test --project=firefox --repeat-each=3
pnpm exec playwright test --project=webkit --repeat-each=3
pnpm exec tsc -b && pnpm lint
pnpm exec playwright test --repeat-each=2
```
