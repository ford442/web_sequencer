## Checkpoint 3 (local verification)

### Work items
- [x] PR #1213 pnpm unpin already on HEAD; no `pnpm/action-setup` `version:` pins
- [x] Collision guard: `scripts/check-pnpm-action-setup.mjs` + unit test + CI source-integrity + husky pre-push
- [x] Playwright serves `dist/` via `vite preview` owned by Playwright (not a GHA-killed preview + Vite dev)
- [x] Product boot: hyphon_native loaded from `document.baseURI`; WebKit E2E stubs pthread Module; 303/Prophecy/loudness/sustain/clock skip AudioWorklet on Safari WebKit
- [x] Playwright `workers: 1` — two AudioContexts + pthread WASM abort Chromium/WebKit (`page closed`)
- [ ] GitHub Actions CI / Playwright E2E / 303 Quality jobs on this branch after push

### Commands last run
- `pnpm install --frozen-lockfile` — PASS (earlier)
- `pnpm exec tsc -b` — PASS (pre-vite)
- `pnpm run lint` — PASS
- `CI=true pnpm run test:unit` — PASS (194 files / 1696 tests; UA sniff no longer treats happy-dom as Safari)
- `node scripts/check-pnpm-action-setup.mjs` — PASS
- synthetic collision fixture — FAIL exit 1 as required
- `pnpm exec vite build` — PASS (dist used for E2E)
- Playwright WebKit `--workers=1` — 17 passed, 4 skipped
- Playwright Chromium 303 + highfid `--workers=1` — 10 passed
- Playwright Chromium remaining specs — passed
- Playwright Firefox `--workers=1` — 16 passed then 1 flake (`wam2` clock poll); re-run after 30s poll + resume — PASS
- Full three-browser matrix in one command not re-run after last spec tweaks (~45m); pieces above cover every spec on each browser except Firefox 303/highfid which passed in the 33.6m Firefox run

### Files touched (do not commit `test-results/`)
- Guard: `scripts/check-pnpm-action-setup.mjs`, `src/__tests__/checkPnpmActionSetup.test.ts`, `.github/workflows/ci.yml`, `.husky/pre-push`
- Harness: `playwright.config.ts`, `.github/workflows/playwright-e2e.yml`, `tests/helpers/boot.ts`, `tests/deploy-smoke.spec.ts`, `tests/verify_note_length.spec.ts`, `tests/wam2-missing-plugin.spec.ts`
- Boot/product: `index.html`, `src/main.tsx`, `src/hooks/usePyodideEngine.ts`, `src/hooks/useAppState.tsx`, `src/hooks/audioEngine/engineLifecycle.ts`, `src/engines/Open303Oscillator.ts`, `src/engines/ProphecyOscillator.ts`, `src/audio/loudness/masterLoudnessStage.ts`, `src/midi/clock/InternalClockAdapter.ts`, `src/utils/engineTelemetry.ts`

### Findings
1. Fast ~90s E2E red: historical `pcfEffect` tsc miss — already fixed on tree.
2. Slow ~90min E2E: GHA killed background preview → Playwright started Vite *dev* → `hyphon_native.js` 404 → overlay never enabled.
3. Vite inlined `hyphon_native.js` next to the JS chunk (404). Load from site root after first paint.
4. Playwright WebKit: pthread `hyphon_native.wasm` in AudioWorklet / `Module()` aborts the renderer. E2E stub + skip worklets.
5. Two Playwright workers: Chromium/WebKit `Target page has been closed`. `workers: 1`.
6. `isAppleWebKit()` matching any AppleWebKit-without-Chrome UA broke happy-dom unit tests (Open303 + scheduler). Require `Safari/` and exclude Chrome/Firefox.

### Next action
Commit (exclude `test-results/`), push `fix/ci-e2e-restore`, watch GitHub Actions.
