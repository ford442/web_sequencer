# Swarm State — E2E Playwright matrix (#1036 / #1039)

## Status
**TESTING** — Firefox AudioContext hang fixed via StartOverlay `?e2e=1` resume patch. Knob smoke green on Firefox ×2. Running full firefox → webkit → chromium matrix.

## Projects
| Project   | Status | Notes |
|-----------|--------|-------|
| chromium  | was green | re-verify after StartOverlay change |
| firefox   | unlocking | hung on `await context.resume()` (state stayed `suspended`); e2e resume() non-blocking patch |
| webkit    | pending | same gesture/resume issue expected |

## Specs / root causes addressed
1. WebKit launch: `--autoplay-policy=…` scoped to chromium only
2. `webServer` block (reuseExistingServer: true)
3. Rack mounts only `selectedTrack` — helpers
4. Module locator: `.hyphon-rack-surface`
5. StartOverlay testids + `?e2e=1` AudioContext.resume() non-blocking patch (Firefox/WebKit)
6. Firefox `firefoxUserPrefs` autoplay belt-and-suspenders
7. `.orig` purge

## Changed since last checkpoint
- StartOverlay: installE2EAudioUnlock (resume never hangs under ?e2e=1)
- playwright.config: firefoxUserPrefs
- Deleted temporary ff-diag specs

## Resume commands
```bash
cd /workspace
git checkout cursor/e2e-playwright-matrix-2ef1
pnpm exec playwright test --project=firefox --workers=1 --repeat-each=2
pnpm exec playwright test --project=webkit --workers=1 --repeat-each=2
pnpm exec playwright test --project=chromium --repeat-each=2
pnpm exec tsc -b && pnpm lint
pnpm exec playwright test --repeat-each=2
```
