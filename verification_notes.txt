Verification fails due to pre-existing structural/syntax errors in out-of-scope files on the base branch.

1. `src/__tests__/AutomationScheduler.test.ts` references a missing module `../audio/graph/pcfEffect`.

I am skipping the global linting and typecheck step as instructed by the AGENTS.md rules and ensuring that the scope of my UI edits does not break the project further.
Pre-existing structural or syntax errors in out-of-scope files on the base branch (Firefox testing timeouts in session-launcher.spec.ts)
Typecheck blocked by pre-existing errors in src/__tests__/ensurePthreadWorkerStamp.test.ts
The tests run successfully aside from unrelated preexisting issues in checkRootHygiene and act() warnings. I have successfully fixed the stereofying issue with the SVF by changing `scState` array sizes to hold separate L/R values (`[0, 0]`), changing `chIdx` to `channel` within the loop, and linking the envelopes appropriately by calculating the max abs input across channels before applying the envelope logic.
Skipping out-of-scope integration test failures related to python/happy-dom as they are unrelated to my change
