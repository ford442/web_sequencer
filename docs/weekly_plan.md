# web_sequencer — Weekly Plan

<!--
FILE ORIENTATION (added 2026-09-14). This file has accumulated duplicate section
headings from earlier runs. The LIVE sections — the ones each weekly run must edit —
are the LATER ones:
  * Today's focus  → the single one near the top (archives prior weeks in <details>)
  * CI toolchain log → the FIRST one (newest entry at the top of its list)
  * Ideas / Backlog / Done / Last run → the SECOND occurrence of each (further down).
The earlier Ideas/Backlog/Done/Last-run blocks are historical and frozen; do not edit
them. Consolidating this file is a standing hygiene item, deliberately not done during
a Fix First run.
-->

## Today's focus
**2026-09-21 — FIX FIRST mode (4th consecutive).** Last week's Fix First **half landed — and the half that didn't is precisely the half that was declared "deliberately not fixed."**

- **P0-a (#1236, native WASM import table) — LANDED**, though by `ab64bd4` (#1256), an independently-written parallel fix, not by the swarm (see `.swarm-state.md`). Derived import table via `WebAssembly.Module.imports()` + warn-once typed stubs in `src/audio-worklets/hyphonNativeImports.ts`, real `emscripten_date_now` / `_emscripten_get_now_is_monotonic`, guard tests running in both the unit and integration tiers.
- **P0-b (red perf tier) — PARTIALLY landed.** `ea4f514` (#1259 Part A) rewrote `SamplerPanel.perf.test.tsx`'s hard-coded render counts as relationships; `e53457b` fixed `perf-summary.json` being empty on disk (collector handoff path + per-file overwrite). **The `.wasm?init` resolution failure was never fixed** — the swarm wrote a fix, then reverted it after reading `docs/PERFORMANCE_BUDGET.md:282` ("Test tier note", added by #1259), which states the perf tier deliberately does not stub WASM.
- **Result: `Test Performance` is red on 41 of 41 runs and has still never been green.** Verified today on run **#41** (`35559956239`, 2026-09-21T04:09Z, HEAD `ebcc802`): 2 failed suites / 3 passed, 33 tests passed, `[ELIFECYCLE] exit code 1`. Identical failure text to run #34 five weeks ago.

Everything else is green on HEAD `07a271f`: **CI #1811 `success`, Playwright E2E #1806 `success`, 303 Quality & Benchmarks #578 `success`.**

**The break, stated precisely.** Two things are both true and jointly fatal:

1. `docs/PERFORMANCE_BUDGET.md:282–292` says the perf tier must **not** stub `.wasm?init`, because the benchmarks need real modules to produce meaningful timings.
2. `.github/workflows/test-perf.yml` has **no `build:wasm` step**, and `src/wasm/*.wasm` is gitignored (`.gitignore:57`).

The perf tier demands real WASM and is never given any. So the correct fix is **neither** of the two things attempted so far — it is to **build the AssemblyScript binaries in the workflow**. Every `.wasm?init` importer in `src/` is AssemblyScript — `WasmOscillator.ts:8` (oscillators), `trackFreezer.ts:13`, `fftLoader.ts:11`, `audioExport.ts:7`, `xmExport.ts:25` — `assemblyscript` is already a devDependency (`package.json:84`), and none of them need Emscripten, Rust, or the `jc303_wasm` submodule toolchain.

**The doc's rationale also names the wrong files**, which is what caused the 09-14 revert. It cites `exportLoudness.perf.test.ts` and `wasmMigration.bench.test.ts` as needing real modules. Both pass today: `exportLoudness` benchmarked fine in run #41 (`median=0.1348ms p95=0.1368ms`), and `wasmMigration.bench.test.ts` uses hand-written JS "Sim" functions and imports no WASM at all. The two that fail are `audioExport.perf.test.ts` and `useAudioEngine.perf.test.tsx`. Correct the doc as part of the fix, or the next run reverts the fix again.

**Primary task, in order:**

1. **Add a WASM build step to `test-perf.yml`** — the five `asc` targets only (`build:wasm:oscillators`, `build:wasm:freezer`, `build:wasm:fft`, `build:wasm:audioexport`, `build:wasm:xmexport`). Explicitly **not** `pnpm run build:wasm`, which also pulls `build:wasm:rust` (wasm-pack) and `build:wasm:jc303` (Emscripten + OpenMP) and would add many minutes plus two toolchain installs to a nightly.
2. **Drive `pnpm run test:perf` to exit 0** — 5 files / ~35 tests, with `perf-summary.json` carrying real samples for every benchmarked function (the 09-14 fix already makes the collector work once the suite passes; `e53457b` verified 28 samples locally).
3. **Correct `docs/PERFORMANCE_BUDGET.md:282–292`** — name the files that actually import WASM, and state the rule going forward: *the perf tier does not stub WASM, it builds it.*
4. **Close the class.** The tier runs only on `schedule` / `workflow_dispatch` / a `perf`-labelled PR, and `.husky/pre-push` runs no tests, which is exactly why it could stay red for six weeks unnoticed. Add failure visibility (file-or-update a tracking issue on a red nightly), and commit the first green run's `perf-summary.json` as the baseline so regressions have something to compare against.
5. **Same hygiene leak, second instance, while you are in here:** `perf-summary.json` (12.7 KB) and `src/perf-summary.json` are both tracked in git as build artifacts.

**Why this over anything else:** this is the 7th consecutive instance of this repo's dominant failure mode — a guard that exists, looks present, and is switched off. It has now survived a dedicated Fix First week, and survived it *because the guard's own documentation argued for leaving it dark*. The perf tier has produced zero baselines in 41 nights, so every ⚡ Bolt performance PR merged this month (#1258, #1267, #1277, #1283, #1284) was measured against nothing at all.

<details><summary>Archived: 2026-09-14 focus — FIX FIRST (#1236 LANDED via #1256; perf tier NOT fixed, carried to 09-21)</summary>

**2026-09-14 — FIX FIRST mode (3rd consecutive).** Last week's 09-07 Fix First **LANDED in full and `main` is genuinely GREEN for the first time since 08-31.** Verified live on HEAD `0a0bcc7`: CI run **#1730** `success`, **Playwright E2E run #1725 `success`** (22 min, the first green E2E since the 12/12 red streak), 303 Quality run #497 `success`. The pnpm collision fix shipped (all `pnpm/action-setup@v4` `version:` pins gone; `packageManager: pnpm@12.3.4` retained at `package.json:108`) **and** the collision guard landed as `scripts/check-pnpm-action-setup.mjs`, wired into `.husky/pre-push` (`install --frozen-lockfile && check-pnpm-action-setup && tsc -b && lint`). The 09-07 backlog's P0-a/P0-b/P0-c are all closed. Owner also closed the entire prior issue board and filed a fresh 8-issue wave on 09-11 (**#1234–#1241**).

**But two foundation breaks are live, and both are the same meta-failure a sixth time: the guard that exists to catch a class is itself dark.**

**P0-a — `hyphon_native.wasm` does not instantiate at all (owner bug #1236).** `createEmscriptenEnv()` (`src/audio-worklets/hyphonNativeImports.ts:224`) builds the WASM import object from a **hand-maintained allowlist**. The shipped binary imports two symbols that are not on it — `emscripten_date_now` and `_emscripten_get_now_is_monotonic` (grep-confirmed absent from the file; the binary declares 28 imports) — so instantiation dies with `LinkError: Import #14 "env" "emscripten_date_now": function import requires a callable`. **Every native 303 and Prophecy voice is silently on the JS fallback in production** — the app still makes sound, so nothing looks wrong. This is the **third instance of the #1176 → #1177 → #1236 "shipped artifact silently doesn't load" family**. The guard test that exists specifically to catch it (`src/__tests__/hyphonNativeImports.test.ts:150–157`) opens with `if (!existsSync(wasmPath)) return;` — and `public/*.wasm` is gitignored while the `test-unit` tier builds no WASM, so **both assertions have been no-ops in CI since they were written.**

**P0-b — the `Test Performance` workflow has been red on 34 of 34 runs since it was created on 2026-08-11. It has never once been green.** Owner spotted half of it in #1237 ("red since at least run `34561165099`, 09-11") — the real history is every run since day one. Three distinct causes in the latest run (`34805038350`, 09-14 04:10Z, job `Vitest perf tier`), 3 failed suites / 5:
1. `src/__tests__/audioExport.perf.test.ts` — `Failed to resolve import "../wasm/audioExport.wasm?init" from src/utils/audioExport.ts` (vite:import-analysis).
2. `src/__tests__/useAudioEngine.perf.test.tsx` — same shape on `../wasm/trackFreezer.wasm?init` from `src/utils/trackFreezer.ts`.
   **(1) and (2) are the 08-17 unit-tier break recurring verbatim.** `vitest.unit.config.ts` carries the `wasm-stub-resolve` `enforce:'pre'` plugin resolving `*.wasm?init` → `src/test/wasmInitStub.ts`; **`vitest.perf.config.ts` has no such plugin**, and `test-perf.yml` never runs `build:wasm`. The fix was never propagated to the perf tier. Owner's #1237 Part A does not cover this half — his patch alone will NOT turn the gate green.
3. `src/__tests__/SamplerPanel.perf.test.tsx` — `expected "spy" to be called 49 times, but got 57 times` ×3 (`:54`, `:72`, `:93`). Hard-coded knob counts that drifted as knobs were added (#1221 drum ducking depth, #1224 lyric stutter, the grain-LFO family). The test's *intent* — an inactive bank's params must not re-render the active bank — is asserted separately and correctly at `:83` (`toHaveBeenCalledTimes(0)`).

Because step 6 fails, **`Collect perf summary` and `Upload perf artifact` have been `skipped` on all 34 runs — `perf-summary.json` has never been produced.** There is no perf baseline data for this project at all, and every regression the tier was built to catch has gone unmeasured for five weeks.

**Primary task — make both dark guards fail loud, in this order:**
1. **#1236, part 1 — derive the import table from the module.** Walk `WebAssembly.Module.imports(module)` in `buildHyphonWasmImports()` and install a typed no-op stub returning `0` for every unimplemented `kind: 'function'` import, `console.warn`-ing once per symbol by name. Keep the existing explicit implementations (`emscripten_resize_heap`, `invoke_*`, `__cxa_throw`, the minified a–z/A–Z aliases) layered on top as overrides. Instantiation can then never again fail on a new Emscripten symbol; at worst a symbol is inert **and says so**.
2. **#1236, part 2 — implement the two real ones properly:** `emscripten_date_now` → `() => Date.now()`, `_emscripten_get_now_is_monotonic` → `() => 1`. Do not leave them as auto-stubs.
3. **#1236, part 3 — make the guard tests fail loud.** Replace `if (!existsSync(wasmPath)) return;` with a deliberate skip gated on an env flag (`HYPHON_REQUIRE_NATIVE=1`, set by the `test-integration` job) plus `expect(existsSync(wasmPath)).toBe(true)` when the flag is on. Add a **no-binary** unit test that synthesizes a tiny `WebAssembly.Module` importing an arbitrary unknown `env` symbol and asserts the derived import object still instantiates — that one runs in the no-WASM tier and guards the derivation logic forever.
4. **Perf gate, part 1 — port the `.wasm?init` stub to the perf tier.** Add the same `wasm-stub-resolve` `enforce:'pre'` plugin to `vitest.perf.config.ts` (reuse `src/test/wasmInitStub.ts`; do not fork it). Then ask the structural question: the plugin now lives in two configs — hoist it into `vitest.shared.ts` (or a small `vitest.wasmStubPlugin.ts`) so the *next* tier added inherits it instead of rediscovering this bug.
5. **Perf gate, part 2 — de-magic the render-count assertions.** Rewrite `SamplerPanel.perf.test.tsx` to capture the baseline render count into a variable and assert relationships (`expect(afterActiveChange).toBe(baseline)`, `expect(afterInactiveChange).toBe(0)`). Sweep the other four perf/bench specs for the same pattern. **This is owner issue #1237 Part A only — do NOT start Part B (the `useAppState` mega-context retirement); that is a multi-PR epic and is out of scope today.**
6. **Verify:** `pnpm run test:perf` exits 0 locally **and `perf-summary.json` is actually written** by `scripts/collect-vitest-perf.mjs`; `pnpm run test:unit` passes with `public/hyphon_native.wasm` present; the full pre-push gate (`install --frozen-lockfile && check-pnpm-action-setup && tsc -b && lint`) is clean; `Test Performance` green on the branch via `workflow_dispatch` before merge.

**Why this over anything else:** `main`'s visible CI is green, and that greenness is exactly the problem — it is green over a native audio engine that does not load and a perf tier that has never run to completion. Both of the guards built to catch these were written, merged, and have been silently inert ever since. Nothing new ships until the WASM import table is self-describing and the perf gate produces its first baseline. **Standing owner action, 5th instance: make the `ci.yml` jobs required branch-protection checks on `main`** — still not done.

<details><summary>Archived: 2026-09-07 focus</summary>

**2026-09-07 — FIX FIRST mode (2nd consecutive).** Last week's 08-31 three-part Fix First **fully LANDED** — all three P0s verified in git and in the working tree:
- **P0-a** — `0608199` (`fix: use remaining queue in SessionLaunchEngine tick partition`). `SessionLaunchEngine.ts:181` now declares `nextQueued`; the `:187`/`:190` use sites agree. Regression coverage added in `src/session/__tests__/sessionLauncher.test.ts` (+25L).
- **P0-b (#1177)** — `PcfEffect.ts:25` and `masterLoudnessStage.ts:10` both converted to `?worker&url` imports, confirming the latent second instance was real (the #1095 ISP limiter + LUFS metering had indeed been dead in production). Class closed: new `src/__tests__/audioWorkletModuleUrls.test.ts` (49L) + an `eslint.config.js` `no-restricted-syntax` guard against raw-`.ts` `addModule` literals + `dist/` worklet assertions in `scripts/check-release-dist.mjs`.
- **P0-c (#1176)** — **also landed**, contrary to `.swarm-state.md` Checkpoint 2, which still lists it open (the state file was never updated after the final push). `28ec7d2` populated `public/hyphon_wasm_export_map.json` (+58L), added the missing-map / zero-entries / stale-identity-map assertions to `check-release-dist.mjs:119–155` (+102L), extended `hyphonNativeImports.ts` (+51L), and added the owner-requested export-name diagnostic logging to `Open303Oscillator.ts` and `ProphecyOscillator.ts` (+45L each).

**But `main`'s entire CI surface is dead — all three workflows, every commit since this morning.** Commit `06ce5b2` ("push fix", a **direct push to `main`**, 2026-09-07 09:44 +0200) added `"packageManager": "pnpm@12.3.4+sha512…"` at `package.json:108` and regenerated `pnpm-lock.yaml` under pnpm 12 (283 lines changed; the `lockfileVersion: '9.0'` file now carries pnpm-12-only `configDependencies` / `packageManagerDependencies` keys). Every workflow pins `pnpm/action-setup@v4` with `version: 9`, and v4 refuses that combination outright:
```
Error: Multiple versions of pnpm specified:
  - version 9 in the GitHub Action config with the key "version"
  - version pnpm@12.3.4+sha512.961aa41… in the package.json with the key "packageManager"
Remove one of these versions to avoid version mismatch errors like ERR_PNPM_BAD_PM_VERSION
```
(run **#1656** / `34099099113`, job `Source Integrity (fail-fast)`, HEAD `6c8c7c6`, 08:09:41Z — the job dies **one second** in, before any checked-out code is touched.) There are **9 such pins across 4 workflows**: `ci.yml` ×5, `playwright-e2e.yml` ×1, `303-quality-benchmarks.yml` ×2, `test-perf.yml` ×1. So `CI`, `Playwright E2E`, `303 Quality & Benchmarks` **and** the scheduled `Test Performance` job all fail in 10–12 s on every push. Last green CI: run **#1646** on `ab8000b` (07:40Z, 4 min). Since `06ce5b2` at 07:47Z, **four merges have landed on a completely dead gate** — #1208, #1209, #1211, #1212.

**And Playwright E2E was already red before that.** All 12 visible `main` runs (09-01 → 09-07) are `failure`, in two distinct shapes: fast ~90 s failures (#1590/#1591/#1593/#1595/#1597) and full ~90 min failures that still upload a 541 MB report (#1583/#1588/#1605/#1607/#1610/#1624/#1630/#1641). Nothing in this plan's history records E2E going red — the 08-03 fix (#1046/#1052) is the last recorded green. **Cause not established this run** — that is work to do, not a claim to carry.

**Today's focus — restore the CI gate, then restore E2E.** Ordered:
1. **P0-a — DONE, shipped in this plan's own PR (#1213).** All 9 `version: 9` pins removed so `pnpm/action-setup@v4` reads `packageManager` from `package.json` (pnpm 12.3.4), matching the lockfile pnpm 12 actually wrote. Verified locally under pnpm 12.3.4: `pnpm install --frozen-lockfile` resolves clean (453 lockfile entries pass supply-chain policy) and all 4 workflow files still parse. Deliberately **not** "fixed" by deleting the `packageManager` field — the committed lockfile is pnpm-12-shaped and pnpm 9 would fight it. kimi-cli picks up from item 2.
2. **P0-b: close the class.** Add a guard to the `source-integrity` job (and to `.husky/pre-push`) that fails when any workflow pins `pnpm/action-setup` `version:` while `package.json` declares `packageManager`. This same family — a manifest/lockfile edit landing without CI validating it — has now red-ed `main` on 06-22, 07-13, and today. Third instance of the family, first of this exact collision.
3. **P0-c: restore green Playwright E2E.** Establish the actual cause from run logs/artifacts *before* changing anything; the two failure shapes are probably two different problems (early config/install failure vs. a genuine test or timeout failure). Fix, then get one full matrix green.
4. **Standing owner action, 4th instance: make the `ci.yml` jobs required branch-protection checks on `main`.** `06ce5b2` was a direct push — no PR, no review, no gate. Carried since #1075 (08-08). Only `ford442` can do this; it is not a code change.

**Boundaries: strictly `.github/workflows/**`, `package.json` (the `packageManager` field only), `pnpm-lock.yaml`, `.husky/`, `scripts/` guard scripts, `playwright.config.ts`, `tests/**`, and `src/` only where an E2E failure proves a real product bug.** `src/audio-worklets/**` stays untouched — that is the Copilot lane. Decoupled Copilot lane = owner issue **#1204**, splitting `src/audio-worklets/open303-processor.ts` (1054 lines) into modules under ~700 lines — zero overlap with CI config, the E2E harness, or the lockfile.

<details><summary>Archived: 2026-08-31 focus — FIX FIRST (SessionLaunchEngine TS2304 + #1176/#1177 — all three LANDED)</summary>

**2026-08-31 — FIX FIRST mode (streak restarts).** Last week's 08-24 New-Idea focus **LANDED**: #1139 RBS song-mode TRAK export + `handleRbsImport` wiring merged as **#1154** (`33a2b54`), and the decoupled Copilot lane — the shadow-playback-stack deletion slice of owner P0 **#1134** — merged as **#1155** (`d4b6b59`, incl. the `pnpm.overrides` `protobufjs ^7.5.8` bump, verified in `package.json:104`). `.swarm-state.md` Checkpoint 1 reports all five acceptance criteria met with `tsc -b` / `lint` / `test:unit` (1596) / `test:integration` / `playwright rbs-automation` all PASS. **But the foundation cracked again on three axes this week, and one of them is a live runtime crash.**

**1 — `main` does not compile (HEAD, today).** Run **#1581** (`33379973535`, 2026-08-31 09:55, HEAD `0f2a5b0`) fails in the `Source Integrity (fail-fast)` job:
```
src/session/SessionLaunchEngine.ts(187,9):  error TS2304: Cannot find name 'nextQueued'.
src/session/SessionLaunchEngine.ts(190,19): error TS2304: Cannot find name 'nextQueued'.
```
Verified in the working tree: `SessionLaunchEngine.ts:181` declares `const remaining: CompiledLaunchEvent[] = []`, but `:187` does `nextQueued.push(e)` and `:190` does `this.queued = nextQueued`. **This is not merely a type error — it is a `ReferenceError` on the audio-clock path.** `tick()` throws the first time any queued launch event is *not* yet due, i.e. the Session Launcher dies in normal use. Introduced by ⚡ Bolt **#1174** (`bcabfff`, 08-30, "Replace array allocations in PhonemePainter and SessionLaunchEngine hot paths") — the `.filter()`→partition-loop rewrite renamed the accumulator in the declaration but not at its two use sites. **#1174's own CI run (#1576) was `cancelled`, so it was never validated before merge** — the exact hole the standing "require CI checks on `main`" owner action has been open on since #1075. Run #1579 (#1175) is red for the same reason; the whole tail of `main` since `bcabfff` is red. Sampled `ci.yml` window on `main`: **11 failure / 11 success / 8 cancelled** — not the "~9 trailing green" the 08-24 run recorded.

**2 — owner bug #1177: AudioWorklet modules 404 under the `/hyphon/` deploy base.** Owner observed live on `test.1ink.us/hyphon` (08-30): `[PcfEffect] Failed to initialize AudioWorklet: AbortError: Unable to load a worklet's module` → PCF bypassed. Root cause is visible in source: `src/engines/PcfEffect.ts:25` builds its module URL as `new URL('../audio-worklets/pcf-processor.ts', import.meta.url).href` — a **raw `.ts` href**. Vite emits no such asset in a production build, so the URL resolves to a non-existent file under `/hyphon/` (works in dev, where TS is served transformed — which is why local smoke tests never caught it). The worklets that *do* work use Vite's `?worker&url` import so a real hashed asset is emitted: `src/midi/clock/InternalClockAdapter.ts:8` (`'../../audio-worklets/clock-processor.ts?worker&url'`) and `src/engines/singing-voice/workletSetup.ts:1` (`rubberband-processor.ts?worker&url`). **Latent second instance — verify, do not assume:** `src/audio/loudness/masterLoudnessStage.ts:21` uses the identical raw-`.ts` `new URL(...)` pattern for `master-loudness-processor.ts`, which would mean the master true-peak limiter + BS.1770-5 LUFS metering shipped in #1095 has been silently dead in production since 08-17.

**3 — owner bug #1176: Open303 + Prophecy fall back to JS on the deployed build.** Same family: deployed-artifact resolution. `Open303Oscillator.ts:27` fetches `resolvePublicAsset('hyphon_native.wasm')` (1.3 MB, threaded — it *does* instantiate), then `fetchExportMap()` → `loadHyphonWasmExportMap()` to resolve the **minified** symbol names (`a.b`, `a.c`, …) back to `open303_*` / `prophecy_*`. On the live build that map comes back empty, the required exports can't be resolved, and both engines degrade — Open303 → `FallbackBassSynth`, Prophecy → JS fallback. Owner's note: *"Explains sequencer/303/prophecy playback feeling dead."* So `hyphon_wasm_export_map.json` is either not emitted into `dist/`, not deployed under `/hyphon/`, or resolved against the wrong base — note `tools/check_wasm_export_map.mjs` runs inside `build:release` and passes locally, so the gate covers the *build* but not the *deployed artifact set*.

**Today's focus — restore green `main`, then fix the `/hyphon/` asset-resolution class that is silently gutting the audio engine in production.** Ordered:
1. **P0-a (minutes): fix `SessionLaunchEngine.ts`.** Rename the `:181` accumulator to `nextQueued` (or the two use sites to `remaining`) so declaration and uses agree; add a unit test asserting `tick()` retains not-yet-due events across a boundary — the case that throws today. `main` is green again the moment this lands.
2. **P0-b (#1177): make every worklet module URL survive the production base.** Convert `PcfEffect.ts:25` — and `masterLoudnessStage.ts:21` if it confirms — to the `?worker&url` import form the working worklets use. Then close the class: a guard test (or lint rule) that fails if any `audioWorklet.addModule` call site resolves a raw `.ts`/`.tsx` path, plus a `dist/` assertion that every registered worklet has an emitted asset. Do not let a worklet failure block engine init (already true for PCF; keep it).
3. **P0-c (#1176): make the native export map reach the deployed build.** Establish empirically whether `hyphon_wasm_export_map.json` lands in `dist/` and is fetched at the right `/hyphon/`-based URL; fix emission/base resolution; extend `scripts/check-release-dist.mjs` so a release build fails when the map is absent or has zero entries. Per the owner's ask, **log the actual export names on mismatch** instead of the current generic degrade message, so the next occurrence is diagnosable from the console alone.
4. **Standing owner action, now with a receipt: make the `ci.yml` jobs required branch-protection checks on `main`.** #1174 merged on a **cancelled** run and broke the build for two days. Carried since #1075 (08-08); this is the third distinct instance. Code cannot fix this — it is a repo-settings change only `ford442` can make.

**Boundaries: strictly `src/session/SessionLaunchEngine.ts`, `src/engines/PcfEffect.ts`, `src/audio/loudness/masterLoudnessStage.ts`, `src/engines/Open303Oscillator.ts` / `ProphecyOscillator.ts` / `hyphonNativeImports.ts`, the release-dist / export-map scripts, and their tests.** The RBS surface, the WAM2 host, the TTS/phoneme work, `emscripten/**` C++ sources, and root-level repo files stay untouched. Decoupled Copilot lane = **root-file hygiene + extending the `check-root.mjs` gate** (15 tracked junk files at repo root — zero overlap with the engine/worklet/session files).

</details>

<details><summary>Archived: 2026-08-24 focus — NEW IDEA (#1139 RBS song-mode — LANDED via #1154)</summary>

**2026-08-24 — NEW IDEA mode.** Last week's 08-17 P0 Fix First (**#1116** — the no-WASM `test-unit` tier couldn't resolve `.wasm?init`) **LANDED & VERIFIED**: `src/test/wasmInitStub.ts` + the `wasm-stub-resolve` `enforce:'pre'` plugin in `vitest.unit.config.ts` map `*.wasm?init` (and bare `*.wasm`) to a hermetic `WebAssembly.Instance` mock; the fix merged via #1142 (run #1507 = `success`). **`main` is GREEN** — HEAD `935486e`/#1149 = `success` (run #1522, 08-23), and the trailing ~9 non-cancelled `ci.yml` runs since #1142 are all green. Three isolated single-commit reds on 08-18/08-19 (#1122 emscripten-6 `check:native`, #1123 `@ts-expect-error` placement, #1126 knob hit-target) were each fixed-forward and sit behind the green HEAD — no systemic break. Foundation solid → **no Fix First** (first non-Fix-First run since 07-27).

**No fresh user-written Ideas (chat-history tools unavailable — 8th consecutive run); the current `Ideas` generated entries (#1033 sampleRate, #1035 non-silent fallback) are superseded by the owner's 08-21 roadmap wave (#1134–#1140). → New Idea mode.** Curated 3 directions from that owner roadmap (not invented): (1) RBS song-mode fidelity #1139, (2) live high-fid 303 A/B + editable diode-ladder #1138, (3) AudioContext sampleRate/`setSinkId` foundation #1136. Picked #1; parked #2/#3 in `Ideas`.

**Today's focus — RBS song-mode fidelity: owner issue #1139 (`feat(rbs): song-mode TRAK export + remaining import→engine wiring`).** The long-flagged "next big theme after CI is green," now owner-scoped. Two shipping-code gaps admit their own incompleteness: `RbsExporter.ts:45` always warns-and-downgrades `mode:'song'` to pattern mode, and `useSongStorage.ts:557` `TODO(#651)` leaves `handleRbsImport` applying only basic pattern/params — not `song.automation`, `rbsMetadata` (tb303*Params/PCF), or the `ImportReport`. Build the song-mode TRAK write-path (must round-trip back through `RbsParser`), finish `handleRbsImport` (apply 303 init params + convert `HyphonAutomationLane[]` into the live automation store + surface `ImportReport` in `RbsImportModal`), handle the 32→8 slot cap without silently dropping chain steps, and land one Playwright path (`tests/rbs-automation.spec.ts`). Self-verifiable via export→import round-trip goldens (corpus snapshots already exist) + the E2E scenario — an ideal long-horizon kimi-cli swarm target. **Boundaries: strictly the RBS import/export + song-storage surface; the real-time audio worklet, WASM, playback hot-paths, CI config, `eslint.config.js`, and the shadow-stack files (Copilot's lane, #1134) stay untouched.** Decoupled Copilot lane = the shadow-playback-stack deletion slice of owner **P0 #1134** (satisfies the owner's "delete shadow stacks before feature epics" gate; expected to merge first — see Open questions).

</details>

<details><summary>Archived: 2026-08-17 focus — FIX FIRST (#1116 `.wasm?init` unit tier — LANDED)</summary>

**2026-08-17 — FIX FIRST mode.** Last week's 08-10 P0 **#1075** (CI parse/build/test integrity) **LANDED**: `ci.yml` now leads with a `source-integrity` fail-fast `tsc -b` job and splits into `lint` / `test-unit` (no-WASM, hermetic fetch guard) / `test-integration` (native build) tiers, and last week's decoupled Copilot lane also shipped (master true-peak limiter + BS.1770-5 LUFS metering, **#1095**). **But `main` is red again on a new axis:** the entire visible `ci.yml` window (08-14→08-17, HEAD `55b03690`) is `failure` — **23 failure / 3 success / 4 cancelled** in the sampled runs. Root cause (run `32025223825`, job "Unit tests (Vitest)"): the new **no-WASM `test-unit` tier cannot resolve static `.wasm?init` imports**. `src/utils/fftLoader.ts` (and `WasmOscillator.ts`, `audioExport.ts`, `xmExport.ts`) do a **top-level** `import initFFT from '../wasm/fft.wasm?init'`; `src/wasm/*.wasm` is gitignored and deliberately **not built** in the unit job, so Vite `import-analysis` throws `Failed to resolve import "../wasm/fft.wasm?init"` the instant the unit tier transforms any spec that transitively imports one — currently `src/utils/__tests__/fft.spec.ts` and `src/engines/rubberband/__tests__/ArtifactDetector.spec.ts` (the latter pulled in by the #1112 ArtifactDetector Bolt refactor). The `source-integrity` fail-fast gate is **blind** to this — `tsc` resolves `*.wasm` via the ambient `declare module '*.wasm'` and never touches Vite resolution.

**Primary task — restore green `main`.** Make the no-WASM `test-unit` tier resolve `*.wasm?init` imports without building binaries: add a Vite resolver/alias (or tiny plugin) in `vitest.unit.config.ts` mapping `*.wasm?init` (and bare `*.wasm`) to a hermetic stub whose default export is an `init()` returning a mock `WebAssembly.Instance`, so **all four** static importers — and any future one — resolve in the unit tier. Prefer this over converting the shipped loaders to lazy `await import()`. Drive the full local gate to a clean exit — `pnpm install --frozen-lockfile && pnpm run test:unit && pnpm run test:integration && pnpm lint && pnpm exec tsc -b` — and fix anything else the tiers surface. Then close the blind spot: the fast lane should also catch unresolved bundler imports (an esbuild/Vite import sweep, or promote `test:unit` to an early gate), and — the standing owner action carried from #1075 — **require the CI checks on `main`** so bot/direct pushes can't red it. No new features until `main` is green and the unit tier is hermetic against `.wasm?init`.

</details>

<details><summary>Archived: 2026-08-10 focus — FIX FIRST (#1075 CI integrity — LANDED)</summary>

**2026-08-10 — FIX FIRST mode.** Last week's 08-03 E2E Fix First **LANDED** (Playwright matrix green, `.orig` artifacts purged, root `weekly_plan.md` removed — merged #1046/#1052; `.swarm-state.md` confirms 84 passed). But the foundation is cracked again on a different axis: **owner `ford442` filed P0 #1075 — "fix(ci): restore parse, build, and test integrity on main" (2026-08-08, labelled "fix before feature work")** — and `main` CI has been red on the majority of the past week's commits: **14 failure / 11 success** in the sampled `ci.yml` window (08-05→08-08 was an almost-unbroken red streak), including a **fresh `tsc` error today** (run `31362669243` on `8707bc3`/#1071: `src/components/note-selector/SynthModulationEffects.tsx` L67/L85 `'currentFormantShift' is possibly 'undefined'` — the un-narrowed `!== undefined || trackType === "voice"` guard is still in the HEAD tree). HEAD `e04a8ee` happens to be green, but type errors keep landing because the pre-push hook can't protect bot/direct pushes and **CI is not a required check on `main`** — the exact recurrence #1075 names.

**Primary task — execute #1075.** Run the full local gate to a clean exit — `pnpm install --frozen-lockfile && pnpm run build:wasm && pnpm exec tsc -b && pnpm lint && pnpm exec vitest run --pool forks` — and fix whatever reproduces: reconcile any remaining duplicated/partially-merged implementations in the four flagged files (`EngineHUD.tsx`, `VoiceManager.ts`, `singing-voice/playback.ts`, `WebGpu303Engine.test.ts`), fix the `SynthModulationEffects.tsx` undefined-guard, confirm `TB303_MODELS` ids are unique (single `gpu-highfid`), split release-vs-debug export-map expectations (`engineInitPaths.test.ts`), and re-run the loudness-budget assertion in isolation before touching its threshold. **Then make the gate fail-fast and enforced:** add a lightweight parser/source-integrity job at the START of `ci.yml` (`tsc -b --pretty false` or an esbuild syntax sweep) so a TS syntax error fails in seconds instead of after minutes of WASM compilation, and require the CI workflow on `main` so bot/direct pushes can't red it. No new features until `main` type-checks + tests clean and CI fails-fast on malformed TS.

</details>

<details><summary>Archived: 2026-07-27 focus — NEW IDEA (Phase-1 TB-303 offline render #974)</summary>

**2026-07-27 — NEW IDEA mode. First green-`main` run in 4+ weeks — the Fix First streak is broken.** Last week's 07-20 Fix First **fully LANDED and is verified live**: `.husky/pre-push` runs the full `install --frozen-lockfile && tsc -b && lint` gate, `src/engines/SingingVoice.ts` carries the scoped eslint-disable for the mixin idiom, `src/hooks/useAudioEngine.ts` `as any` is stripped to **0**, and **the last 8 CI runs on `main` are all `success`** (HEAD `2e3af39`, run #1220, 2026-07-27). The recurring gate-break class (lockfile → lockfile → lint, 3 breaks running) is finally closed by the guardrail that now exists. No cracked foundation → **no Fix First.**

**The `Ideas` section holds no fresh user entries (only `[done]` items) → New Idea mode.** Synthesized 3 directions from live repo state + active focus areas; picked one; appended the two unpicked to `Ideas`.

**Today's focus — Phase-1 of Epic #972: OpenMP oversampling + offline 303 worker-pool (issue #974).** Phase-0 (#973) landed *today* (#1001/#1002): `docs/audio-engine/303-authenticity-gaps.md` + the jc303 soft-oracle baseline + vs-reference spectral metrics (`docs/audio-engine/303-baseline-spectra/baseline_metrics.json`) now exist as ground truth. #974 is unblocked and is exactly where the momentum sits — 2 of the last 3 `main` commits are 303-authenticity work. Build the **offline** render path per #974: an `OVERSAMPLE_FACTOR` (1/2/4) toggle in `emscripten/audio_dsp.cpp`, an `Offline303Renderer.worker.ts`, a thin `render303Offline()` API, and telemetry — **strictly offline / freeze / export only; the real-time AudioWorklet path stays untouched** (Epic principle #1). It's self-verifiable against the Phase-0 baselines (render → spectrogram diff → compare to the committed metrics), which is what makes it an ideal long-horizon kimi-cli swarm target.

</details>

<details><summary>Archived: 2026-07-20 focus — FIX FIRST (LANDED; `main` now green)</summary>

**2026-07-20 — FIX FIRST mode (4th consecutive).** Last week's 07-13 Fix First **LANDED the hard part**: `@babel/helper-plugin-utils` is gone from `package.json` **and** `pnpm-lock.yaml` was regenerated (install now passes — the `ERR_PNPM_OUTDATED_LOCKFILE` break is resolved), so the `build:wasm → tsc -b → lint → vitest` gate finally **runs on main for the first time**. The tsc step **passes** — the 62-commit merge spree's type drift got hand-patched across #943–#947 + three `push fix` commits, and `as any` in `useAudioEngine.ts` is down 16→13.

**But `main` is STILL red — the gate now fails at the `pnpm lint` (ESLint) step**, and it's been red for a full week (07-13→07-19, HEAD run **#1097 / 29688357867**, commit `7bea600`, 2026-07-19 13:09Z, conclusion `failure`). **62 commits merged onto red `main` since 07-13** — none CI-validated. Exact cause (from run #1097 `Lint + Test` job 88196564266):
- `src/engines/SingingVoice.ts` — **3 ESLint errors**, all on the mixin declaration-merge pattern (`export interface SingingVoice extends SingingVoicePublic {}` L51 + `export class SingingVoice implements SingingVoicePublic` L53):
  - `51:18  @typescript-eslint/no-empty-object-type` — interface declaring no members ≡ its supertype
  - `51:18  @typescript-eslint/no-unsafe-declaration-merging`
  - `53:14  @typescript-eslint/no-unsafe-declaration-merging`
- ESLint stops reporting per file-group, so **once `SingingVoice.ts` is clean, more lint errors from the un-validated spree may surface** — the gate has to be run to a clean exit locally, not just this one file.

This is the same meta-failure a **fourth time**: a change lands, `main` goes red, the merge spree keeps stacking on top. Last week it was the lockfile; this week it's lint. The standing backlog item — a **pre-push gate hook** — would have caught every one of them, and it *still doesn't exist*.

**Primary task — restore green `main`, then finally install a pre-push gate that runs the whole thing:**
1. **Fix the 3 ESLint errors in `src/engines/SingingVoice.ts` (L51/L53).** The `interface X extends Public {}` + `class X implements Public` pair is an **intentional** mixin-typing idiom (methods are attached via `Object.assign` below, per the L50 comment) — do NOT delete it. Silence the two rules narrowly with `// eslint-disable-next-line @typescript-eslint/no-empty-object-type, @typescript-eslint/no-unsafe-declaration-merging` on L51 (and the merge rule on L53), OR restructure to type the mixin surface without the empty-interface merge if a clean idiom exists. Prefer the scoped disable — it documents intent.
2. **Run the full gate locally to a clean exit and fix EVERYTHING it surfaces** — `pnpm install --frozen-lockfile && pnpm run build:wasm && pnpm exec tsc -b && pnpm lint && pnpm test`. Fix any additional lint errors beyond `SingingVoice.ts`, any tsc drift, any vitest failures introduced by the 62-commit spree. Strip the last **13 `as any`** in `useAudioEngine.ts` against `src/types.ts` (add a genuinely-missing prop only if truly absent — do not re-add casts).
3. **Install the pre-push gate guardrail** (husky `.husky/pre-push` running `pnpm install --frozen-lockfile && pnpm exec tsc -b && pnpm lint`, and/or a dedicated PR `lockfile-check` + lint job). This is the guardrail the backlog has carried since 06-22 across 3 breaks (lockfile → lockfile → lint) — a *full* pre-push gate closes all three classes at once. This finally clears the standing "Green-CI guardrail" item.
4. **Verify:** all five gate steps clean on the fix branch, both live workflows (`CI`, `Playwright E2E`) green before merge.

**Why this over anything else:** `main` has been red for a week and 62 commits deep in unvalidated merges. The gate you built is working exactly as designed — it caught real lint debt — but nothing can merge safely until it's green, and the recurring cause is that the gate is never run *before* push. Close it with the hook. No new features until `main` is green and the pre-push gate exists so this is the *last* time an un-run gate reds `main`.

<details><summary>Archived: 2026-07-13 focus</summary>

**2026-07-13 — FIX FIRST mode.** Last week's escalated CI-hardening **largely EXECUTED this time** (unlike 06-29, which never ran): `ci.yml` now has a **`Type-check (tsc)` step** (`pnpm exec tsc -b`), **lint is un-masked** (`continue-on-error: true` gone — comment now reads "failures here must be fixed rather than masked"), **`debug_build.yml` was deleted** (perma-red workflow resolved by removal; only `ci.yml` + `playwright-e2e.yml` remain), and **`@babel/helper-plugin-utils` was dropped from `package.json`**. `useAudioEngine.ts` is back to a 1048-line file with `as any` down 21→16. Real progress.

**But the foundation is cracked again — and it's the exact recurrence the backlog warned about.** Dropping `@babel/helper-plugin-utils` from `package.json` was **not accompanied by a lockfile regen**, so `pnpm-lock.yaml` still carries the orphan entry (5 refs). Live evidence from `main` run **29243396503** (job 86794492928, 2026-07-13 10:38Z):
- Step 7 **`pnpm install --frozen-lockfile` FAILS** in ~1s with **`ERR_PNPM_OUTDATED_LOCKFILE — pnpm-lock.yaml is not up to date with package.json`**. Failure reason names the exact delta: the lockfile specifier set includes `@babel/helper-plugin-utils` but `package.json` does not.
- **Every downstream gate is SKIPPED**: `Build WASM`, **`Type-check (tsc)`**, `Lint (ESLint)`, `Test (Vitest)` — all `skipped`. The type-gate that landed this week **has never actually run on main**; install dies before it.
- **All recent main runs are `failure`** (#1017–#1031, 2026-07-13) — the merge spree (#909/#914/#915/#917/#918/#919/#921) has been landing on a red, install-broken `main`, so none of it is CI-validated. In-flight fix commits (`eb70561` "fix remaining lint errors", `d043baa` "missing commas in Harmonizer") were never gate-checked either.

This is the **second lockfile-caused CI break** (first was 2026-06-22). The standing backlog item — a `pnpm install --frozen-lockfile` pre-push hook — would have caught both.

**Primary task — restore green `main`, then finally install the guardrail so this can't recur:**
1. **Regenerate `pnpm-lock.yaml`** so it matches `package.json` (run `pnpm install` locally to drop the orphan `@babel/helper-plugin-utils` entry — 5 refs — then commit the lockfile). Nothing else should change in the lockfile.
2. **Let the now-live gate run for the first time.** Once install passes, `build:wasm → tsc -b → lint → vitest` execute on main for the first time in a week. **Fix whatever real errors they surface** — residual type drift, lint failures from the un-validated merge spree, the last 16 `as any` in `useAudioEngine.ts` (strip them against `src/types.ts`; add a genuinely-missing prop only if one is actually absent — do not re-add casts).
3. **Install the pre-push / pre-merge `--frozen-lockfile` guardrail** (husky pre-push hook or a dedicated `lockfile-check` CI job that runs `pnpm install --frozen-lockfile` on PRs) so a stale lockfile fails **before** merge, not after. This closes the standing "Green-CI guardrail" backlog item for good.
4. **Verify:** `pnpm install --frozen-lockfile` clean, `pnpm exec tsc -b` clean (0 errors), `pnpm run build` green, `pnpm test` green, and both live workflows (`CI`, `Playwright E2E`) green on the fix branch before merge.

**Why this over anything else:** `main` has been red at the install step for a week; the CI gate you just built cannot protect anything because it never runs. Regenerating one lockfile un-blocks the entire gate and reveals the true state of the merge spree. No new features until install is green, the gate has run, and the lockfile guardrail exists so this is the *last* time a manifest edit silently reds `main`.

</details>

</details>

</details>
</details>

## CI toolchain log
- **2026-09-21 — reconcile: 09-14 Fix First HALF landed; the perf tier is red for the 41st consecutive run.** #1236 landed via `ab64bd4` (#1256, parallel independent fix) and the `perf-summary.json` write path landed via `e53457b`; **the `.wasm?init` resolution failure was reverted, not fixed**, on the strength of `docs/PERFORMANCE_BUDGET.md:282`'s "the perf tier intentionally does not stub WASM". Root cause finally pinned: that statement is correct *and* `test-perf.yml` never builds the WASM it therefore requires. All five `.wasm?init` importers are AssemblyScript and `assemblyscript` is already a devDependency — the fix is a build step, not a stub. Verified green elsewhere on HEAD `07a271f`: CI #1811, Playwright E2E #1806, 303 Quality #578.
- **2026-09-14 — reconcile: 09-07 Fix First LANDED in full; `main` is GREEN — and two dark guards are the new break.** Verified live on HEAD `0a0bcc7`: CI **#1730** `success`, **Playwright E2E #1725 `success`** (22 min — first green E2E since the 12/12 red streak began ≥09-01), 303 Quality #497 `success`. PR #1213 shipped the `pnpm/action-setup` unpin; `packageManager: pnpm@12.3.4` stays at `package.json:108`; the collision guard landed as `scripts/check-pnpm-action-setup.mjs` and is wired into `.husky/pre-push`. Owner closed the whole prior board (#1134–#1140, #1176/#1177, #1181, #1203/#1204, #1030–#1039 …) and filed a fresh 8-issue wave on 09-11: **#1234–#1241**. **NEW BREAK ×2, both the same shape — a guard that was merged and has been inert ever since.** (a) **#1236:** `createEmscriptenEnv()` (`hyphonNativeImports.ts:224`) builds the WASM import object from a hand-maintained allowlist missing `emscripten_date_now` + `_emscripten_get_now_is_monotonic`, so `hyphon_native.wasm` **fails to instantiate** (`LinkError: Import #14 "env" "emscripten_date_now"`) and every 303/Prophecy voice runs the JS fallback in production. The guard test (`hyphonNativeImports.test.ts:150–157`) starts `if (!existsSync(wasmPath)) return;` — `public/*.wasm` is gitignored and `test-unit` builds no WASM, so it has **always** passed vacuously. Third instance of the #1176 → #1177 → #1236 family. (b) **`Test Performance` is red on 34/34 runs since its creation 2026-08-11 — never once green.** Latest run `34805038350`: `audioExport.perf.test.ts` and `useAudioEngine.perf.test.tsx` both die at `vite:import-analysis` on `*.wasm?init` (**the 08-17 unit-tier break, recurring — `vitest.perf.config.ts` never got the `wasm-stub-resolve` plugin that `vitest.unit.config.ts` has**), plus `SamplerPanel.perf.test.tsx` `toHaveBeenCalledTimes(49)` vs 57 actual. Owner's #1237 Part A addresses only the magic numbers and will not turn the gate green on its own. `Collect perf summary` / `Upload perf artifact` have been `skipped` all 34 runs → **`perf-summary.json` has never existed; there is no perf baseline for this project.** Today = **FIX FIRST**: derive the import table from the module + fail-loud native guard → port the wasm stub to the perf tier and hoist it into shared config → de-magic the render-count assertions. **Standing owner action, 5th instance: required branch-protection checks on `main`.**
- **2026-09-07 — reconcile: 08-31 three-part Fix First LANDED in full; NEW break — the pnpm `packageManager` / `action-setup version: 9` collision kills all three workflows.** All three 08-31 P0s verified landed: P0-a via `0608199` (`SessionLaunchEngine` accumulator + `sessionLauncher.test.ts` regression case); P0-b via `28ec7d2` (`PcfEffect.ts` **and** `masterLoudnessStage.ts` → `?worker&url`, confirming the latent second instance was real; `audioWorkletModuleUrls.test.ts` + an `eslint.config.js` `no-restricted-syntax` guard + `dist/` worklet assertions); **P0-c also landed in `28ec7d2`** despite `.swarm-state.md` Checkpoint 2 still listing it open — `public/hyphon_wasm_export_map.json` populated, `check-release-dist.mjs:119–155` now fails a release build on a missing / zero-entry / stale-identity map, `hyphonNativeImports.ts` +51L, and export-name diagnostic logging added to `Open303Oscillator.ts` / `ProphecyOscillator.ts`. **NEW BREAK — the whole CI surface is dead.** `06ce5b2` ("push fix", **direct push to `main`**, 09-07 09:44 +0200) added `"packageManager": "pnpm@12.3.4+sha512…"` (`package.json:108`) and regenerated `pnpm-lock.yaml` under pnpm 12 (283L; the `lockfileVersion: '9.0'` file now carries pnpm-12-only `configDependencies`/`packageManagerDependencies` keys). All 9 `pnpm/action-setup@v4` call sites across `ci.yml` (×5), `playwright-e2e.yml`, `303-quality-benchmarks.yml` (×2) and `test-perf.yml` pin `version: 9`; v4 hard-errors on `Multiple versions of pnpm specified` and every job dies ~1 s in (run **#1656** / `34099099113`, `Source Integrity (fail-fast)`, 08:09:41Z). Last green CI = run **#1646** on `ab8000b` (07:40Z); **four merges have since landed on a dead gate** (#1208/#1209/#1211/#1212). **Separately, Playwright E2E has been red on 12/12 `main` runs since at least 09-01**, in two shapes (fast ~90 s vs. full ~90 min with a 541 MB report upload) — cause not yet established. Today = **FIX FIRST**: unpin `version: 9` → guard the collision class in `source-integrity` + `.husky/pre-push` → root-cause and restore E2E. **Standing owner action, 4th instance: required branch-protection checks on `main`** — this break arrived as a direct push with no PR at all.
- **2026-08-31 — reconcile: 08-24 New Idea (#1139 RBS song-mode) LANDED; `main` is RED again + two owner production bugs.** #1139 merged as **#1154** (`33a2b54`) and the Copilot lane P0 #1134 slice merged as **#1155** (`d4b6b59`, protobufjs override verified at `package.json:104`); `.swarm-state.md` Checkpoint 1 shows all 5 acceptance criteria met, 1596 unit tests green. **NEW BREAK — `main` HEAD does not compile.** Run **#1581** (`33379973535`, 08-31 09:55, `0f2a5b0`), job `Source Integrity (fail-fast)`: `src/session/SessionLaunchEngine.ts(187,9)` + `(190,19)` **TS2304 Cannot find name 'nextQueued'** — `:181` declares `remaining`, `:187`/`:190` use `nextQueued`. **Also a runtime `ReferenceError` in `tick()`** whenever a queued launch event is not yet due, so the Session Launcher is broken, not just untyped. Introduced by ⚡ Bolt **#1174** (`bcabfff`, 08-30) whose own CI run **#1576 was `cancelled`** — merged unvalidated. Sampled `ci.yml` window on `main`: **11 failure / 11 success / 8 cancelled** (not the ~9 trailing green recorded on 08-24). **Plus two owner `bug` issues filed 08-30 from the live `/hyphon` deploy, both in one family — asset resolution under the deploy base:** **#1177** `PcfEffect.ts:25` builds its worklet URL as a raw `.ts` `new URL(..., import.meta.url)` that Vite never emits → `AbortError: Unable to load a worklet's module` (the working worklets — `InternalClockAdapter.ts:8`, `singing-voice/workletSetup.ts:1` — use `?worker&url`; `audio/loudness/masterLoudnessStage.ts:21` carries the same raw-`.ts` pattern and is a likely silent second casualty since #1095). **#1176** `hyphon_native.wasm` instantiates but `loadHyphonWasmExportMap()` returns empty on the deployed build, so minified `open303_*`/`prophecy_*` symbols can't be resolved → `FallbackBassSynth` + Prophecy JS fallback ("explains sequencer/303/prophecy playback feeling dead"). `tools/check_wasm_export_map.mjs` gates the *build*, not the *deployed artifact set*. Today = **FIX FIRST**: SessionLaunchEngine → worklet URL class (#1177) → export-map delivery (#1176). **Standing owner action, third instance and now with a receipt: make the `ci.yml` jobs *required* branch-protection checks on `main`** — a cancelled run merged the break.
- **2026-08-24 — reconcile: 08-17 `.wasm?init` unit-tier Fix First LANDED & VERIFIED — `main` is GREEN.** Verified live: `src/test/wasmInitStub.ts` exists and `vitest.unit.config.ts` carries the `wasm-stub-resolve` `enforce:'pre'` plugin resolving `*.wasm?init`/`*.wasm` → the stub; the fix merged via #1142 (run #1507 `success`). HEAD `935486e`/#1149 = `success` (run #1522, 08-23); the trailing ~9 non-cancelled `ci.yml` runs since #1142 are green. Three isolated single-commit reds (08-18 #1122 emscripten-6 `check:native`; 08-19 #1123 `@ts-expect-error` placement, fixed by #1124; 08-19 #1126 knob hit-target) were each fixed-forward and are behind the green HEAD — no systemic break. Foundation solid → first non-Fix-First run since 07-27. Owner filed a 7-issue roadmap wave on 08-21 (#1134–#1140); today = **New Idea**, focus = **#1139 RBS song-mode fidelity** (kimi-cli), Copilot lane = shadow-stack deletion slice of P0 **#1134**. **Standing owner action still open (carried from #1075):** make the CI checks *required* branch-protection on `main` — bot/direct pushes can still red it (the 08-18/19 reds landed directly on `main`).
- **2026-08-17 — reconcile: 08-10 P0 #1075 CI-integrity Fix First LANDED; NEW break — no-WASM unit tier can't resolve `.wasm?init`.** Verified live in `ci.yml`: the fail-fast `source-integrity` (`tsc -b --pretty false`) job leads, gating `lint` / `test-unit` (no-WASM, hermetic fetch guard via `vitest.setup.unit.ts`) / `test-integration` (native `build:native:changed` + export-map check + offline-303 host g++). Last week's decoupled Copilot lane also shipped — master true-peak (ISP) limiter + BS.1770-5 LUFS metering (#1095). **NEW BREAK:** `main` red across the whole sampled window (08-14→08-17, HEAD `55b03690`) — **23 failure / 3 success / 4 cancelled**. The `test-unit` job fails at Vite `import-analysis`: `Failed to resolve import "../wasm/fft.wasm?init" from src/utils/fftLoader.ts` (run `32025223825`, job "Unit tests (Vitest)"). Cause: the no-WASM unit tier transforms specs (`fft.spec.ts`, `rubberband/ArtifactDetector.spec.ts`) that transitively import 1 of the **4 top-level `.wasm?init` importers** (`fftLoader.ts`, `WasmOscillator.ts`, `audioExport.ts`, `xmExport.ts`), but `src/wasm/*.wasm` is gitignored and not built in that job. `source-integrity`'s `tsc` gate is blind to it (ambient `declare module '*.wasm'`). Today = **FIX FIRST**: stub `*.wasm?init` in `vitest.unit.config.ts`, drive both test tiers + lint + tsc to clean exit, teach the fast lane to catch unresolved bundler imports, and (standing owner action) require CI on `main`.
- **2026-08-10 — reconcile: 08-03 E2E Fix First LANDED; NEW break — main CI integrity (P0 #1075).** Verified live: `.orig` merge artifacts gone from `src/`, root `weekly_plan.md` removed, `playwright.config.ts` has the `webServer`/`reuseExistingServer` block (merged #1046 + knob-click follow-up #1052); `.swarm-state.md` reports the full matrix green (84 passed). **BUT** owner `ford442` filed **P0 #1075** ("restore parse, build, and test integrity on main", 2026-08-08) and `main` `ci.yml` has been red on the majority of the week's commits — **14 failure / 11 success / 5 cancelled** in the sampled window; 08-05→08-08 an almost-unbroken red streak; a fresh `tsc` error red-ed `8707bc3`/#1071 today (`SynthModulationEffects.tsx` L67/L85 `currentFormantShift` possibly undefined). Root cause is structural and recurring: `ci.yml` runs `tsc` at step 70 **after** minutes of WASM build (no fail-fast parser job), and CI is **not a required check on `main`**, so bot/direct pushes keep landing type errors. Today = **FIX FIRST on #1075** (drive the local gate to clean exit + add fail-fast parser job + require CI on main). The 07-27 New-Idea focus (Phase-1 TB-303 #974) is **superseded/paused** — foundation first.
- **2026-07-27 — reconcile: 07-20 lint Fix First LANDED & VERIFIED — `main` is GREEN, streak broken.** Verified live on `origin/main` (HEAD `2e3af39`): `.husky/pre-push` exists and runs `pnpm install --frozen-lockfile && pnpm exec tsc -b && pnpm lint`; `package.json` has `prepare: husky` + `husky@^9.1.7`; `src/engines/SingingVoice.ts` L51/L54 carry the scoped `eslint-disable` for the mixin declaration-merge; `src/hooks/useAudioEngine.ts` `as any` count = **0** (was 13). **The last 8 `ci.yml` runs on `main` are all `success`** (runs #1220/#1218/#1216/#1214/#1211/#1209/#1203/#1199, 2026-07-26→27), including today's HEAD. The `.swarm-state.md` from the 07-20 swarm confirms all 5 gate steps green (1280 tests). This closes the **standing "Green-CI guardrail" backlog item carried since 06-22** across 4 Fix First runs (lockfile 06-22 → parse/type 07-06 → lockfile 07-13 → lint 07-20) — the pre-push full-gate hook now prevents all three break classes. **First non-Fix-First run in 4+ weeks.**
- **2026-07-20 — reconcile: 07-13 lockfile Fix First LANDED the install fix; red moved to lint.** Verified live on `origin/main` (HEAD `7bea600`): `@babel/helper-plugin-utils` gone from `package.json`, `pnpm-lock.yaml` regenerated → **`pnpm install --frozen-lockfile` passes**, the `build:wasm → tsc -b → lint → vitest` gate **runs on main for the first time**, and **`tsc -b` passes** (the 62-commit spree's type drift was hand-patched across #943–#947 + 3 `push fix` commits; `as any` 16→13). **NEW/REMAINING BREAK:** the un-masked `pnpm lint` step now **fails main** — 3 ESLint errors in `src/engines/SingingVoice.ts` L51/L53 (`no-empty-object-type` + `no-unsafe-declaration-merging` ×2 on the intentional mixin declaration-merge). Run #1097 (29688357867, job 88196564266, 2026-07-19 13:09Z) = `failure` at lint; main red 07-13→07-19; **62 commits merged onto red main, none CI-validated.** This is the 3rd distinct gate break in a row (lockfile 06-22 → lockfile 07-13 → lint 07-20) and the **pre-push gate guardrail still doesn't exist.** Today's Fix First = fix SingingVoice lint (+ any lint/tsc/test the spree added) → strip last 13 `as any` → install a full pre-push gate hook.
**Why this over anything else:** every other audit signal is clean (#720 closed, knob work shipped). The only cracked thing is the build gate, and it's invalidating a week of merged work silently.
**2026-07-06 — FIX FIRST mode (escalated).** Last week's 2026-06-29 Fix First was a **planning run only** — the dispatch was generated and the plan commit merged (PR #819), but the kimi-cli swarm to execute the CI type-gate hardening **never ran**. `.swarm-state.md` is still the stale 2026-06-22 CI-restoration state; nothing from 06-29 landed. Meanwhile the merge spree kept going (#826/#828/#848/#850…), and the foundation degraded from "green CI can't see type drift" to **"the primary audio hook does not parse."**

**Hard evidence gathered this run:**
- **`src/hooks/useAudioEngine.ts` has a genuine syntax error** — `esbuild` fails with `Expected ")" but found ";"` at **line 1013** (unbalanced `(` in the `playSamplerVoice` / `playBufferSource` region above it). The file **does not compile**. PR #850 (Jules, today) confirms it independently: *"useAudioEngine.ts has a pre-existing bracket/parse issue on main which continues to cause linting errors."*
- **CI masks it three ways:** `ci.yml` runs `lint` with **`continue-on-error: true`** (so the parse error is swallowed), still runs **no `tsc`** at all, and vitest degrades gracefully around missing WASM — so `main`'s "Lint + Test" badge is green over a non-parsing hot-path file. Perf PRs keep landing on top of it.
- **`debug_build.yml` is RED on every main run** (confirmed 3/3 latest: runs 28778224743 / 28777879606 / 28777465392) — the **`Setup Rust` step is still mis-indented** (6-space nested under `setup-node`'s `with:`). Never fixed. A perma-red check masks any *new* red X.
- **21 `as any` casts** now in `useAudioEngine.ts` (was 4 last week) — the type surface has been fully defeated in the audio hot path.
- **`@babel/helper-plugin-utils`** still in `package.json` (line 38) despite `.swarm-state.md` claiming it was removed — the stale-state/reality gap.

**Primary task — repair the audio-engine foundation, then install the gate that would have caught it:**
1. **Fix the parse error in `src/hooks/useAudioEngine.ts`** (start at the `esbuild` pointer L1013 `Expected ")"`; find the unbalanced `(` above in the `playSamplerVoice`/`playBufferSource` region). The file must parse and `tsc -b` clean. **This is #1 — nothing else matters until it compiles.**
2. **Strip the 21 `as any` casts** in that file once it parses; restore real typing against `src/types.ts` (add a genuinely-missing prop to `types.ts` only if one is actually absent — do not paper over with casts).
3. **Add a `tsc -b` step to `ci.yml`** (after `build:wasm`) so a non-parsing / type-broken file fails the **main** gate, not just the flaky E2E build.
4. **Flip `lint` off `continue-on-error: true`** in `ci.yml` once the file lints clean — the swallowed-error mode is what let this hide.
5. **Fix `debug_build.yml`** — correct the `Setup Rust` indentation to a sibling step (or delete the workflow if redundant with `ci.yml`); confirm it loads and goes green.
6. **Drop unused `@babel/helper-plugin-utils`** from `package.json` devDeps.
7. **Verify:** `pnpm exec tsc -b` clean (0 errors), `pnpm run build` green, all three workflows (`CI`, `Playwright E2E`, `debug_build`) green on the fix branch before merge.

**Why this over anything else:** `main` is shipping a non-compiling primary audio hook behind a green badge, and every perf PR this week rebased onto it. This is the definition of a cracked foundation. No new features until the hook parses, the `as any` band-aids are gone, and `ci.yml` actually type-checks + fails on lint errors.

## CI toolchain log
- **2026-07-13 — reconcile: last week's 2026-07-06 escalated Fix First MOSTLY EXECUTED — but the babel-dep removal re-broke install.** Verified live on `origin/main` (HEAD `9c9e850`): (a) **`ci.yml` now has a `Type-check (tsc)` step** (`pnpm exec tsc -b`) — the primary goal landed ✅; (b) **lint un-masked** — `continue-on-error: true` removed, comment now "failures here must be fixed rather than masked" ✅; (c) **`debug_build.yml` deleted** — the perma-red workflow resolved by removal (only `ci.yml` + `playwright-e2e.yml` remain) ✅; (d) **`@babel/helper-plugin-utils` removed from `package.json`** ✅; (e) `useAudioEngine.ts` present at 1048 lines, `as any` down 21→16 (partial). **NEW BREAK:** the babel removal was **not** paired with a lockfile regen → `pnpm-lock.yaml` still lists `@babel/helper-plugin-utils` (5 refs) → `pnpm install --frozen-lockfile` fails with **`ERR_PNPM_OUTDATED_LOCKFILE`** on every main run (run 29243396503 / job 86794492928, 10:38Z). All downstream steps — `build:wasm`, `tsc`, `lint`, `vitest` — **SKIPPED**. So the new gate has literally never run on main, and #909–#921 merged onto a red/unvalidated `main`. This is the 2nd lockfile-caused break (1st = 06-22); today's escalated Fix First = regen lockfile → run the live gate → fix surfaced errors → install the `--frozen-lockfile` guardrail.
- **2026-07-06 — reconcile: last week's 2026-06-29 Fix First DID NOT EXECUTE.** Only the plan commit merged (PR #819); the kimi-cli CI-hardening swarm never ran. `.swarm-state.md` is still the stale 2026-06-22 state. Consequences verified live this run: (a) `ci.yml` **still has no `tsc`** and still runs `lint` with **`continue-on-error: true`**; (b) `debug_build.yml` **still perma-red** on every main run (`Setup Rust` still mis-indented — 3/3 latest runs = failure); (c) `@babel/helper-plugin-utils` **still in `package.json`**; (d) **NEW & worse — `src/hooks/useAudioEngine.ts` no longer parses** (`esbuild`: `Expected ")" but found ";"` @ L1013), independently confirmed by Jules in PR #850. The `as any` count in that file grew 4 → **21**. Escalated Fix First; see Today's focus.
- **2026-06-29 — reconcile: last week's CI fix LANDED (different fork than logged).** The version that actually merged to `main` **pinned `vitest` back to `2.1.9`** and kept `vite ^5.4.21` (the conservative fork from `.swarm-state.md`, NOT the vitest-4/vite-6 forward migration logged below). `@babel/helper-plugin-utils` is **still present** in `package.json` and **still unused in `src/`** (grep-confirmed) — the logged "dropped" never happened; carry as a tiny cleanup. Net: main `ci.yml` install/lint/test gate is green and stable. **Gap exposed:** `ci.yml` runs `build:wasm → lint → vitest` only — **no `tsc`**. That's today's FIX FIRST.
- **2026-06-22 — vitest 4 + vite 6 forward migration (the FIX FIRST primary task).** Chose the *migrate-forward* fork over pinning vitest back. Changes:
  - `package.json`: `vite ^5.4.21 → ^6.3.6` (vitest 4 requires vite 6's `./module-runner` export — the root of the `ERR_PACKAGE_PATH_NOT_EXPORTED` crash), kept `vitest ^4.1.9`, **dropped `@babel/helper-plugin-utils`** (referenced nowhere in source — accidental dep from #793). Regenerated `pnpm-lock.yaml` (now resolves vite 6.4.3 / vitest 4.1.9; `--frozen-lockfile` will pass).
  - **vitest 4 mock breakage:** vitest 4 invokes `vi.fn().mockImplementation(...)` via `Reflect.construct` when used with `new`, so arrow-function implementations throw "is not a constructor". Converted the constructable mocks to `function` expressions in `vitest.setup.ts` (`AudioContext`, `Worker`), `SingingVoiceManager.test.ts` (`SingingVoice`), `VoiceEditor.test.tsx` (`VoiceDesigner`), `Open303Config.test.ts` (`AudioWorkletNode`).
  - **Genuine pre-existing bug surfaced & fixed:** esbuild 0.25 (pulled in by vite 6) rejected `src/hooks/audioEngine/audioPlayback.ts` with `Unexpected "}"` at line 562 — a real orphaned closing brace in `createPlayDrum` left behind when the "retrigger loop" was removed (line 428 comment). `tsc` agrees (TS1128). esbuild 0.21 under vite 5 had silently tolerated it; this was never validated because CI couldn't install. Removed the stray brace.
  - **Result:** full suite green under vitest 4 / vite 6 — **1128 passed, 3 skipped, 104 files (1 skipped)**. Validated locally after building the 5 AssemblyScript wasm targets (`oscillators`, `trackFreezer`, `fft`, `audioExport`, `xmExport`) that suites import via `?init` (CI's `Build WASM` step does this).
  - **Still red elsewhere (NOT this migration's scope, separate merge-spree fallout):** `tsc -b` reports pre-existing errors in untouched files — `NoteSelector.tsx` (`"vocoderMix"` not in param union), node-builtin/`global`/`Buffer` type resolution in some test files (`engineInitPaths`, `trackFreezer`, `CloudStatus`, `xmWriter`). These block `tsc -b && vite build` and need a follow-up pass.

## GPU compute log
- **2026-06-22 — WebGpuBackend GPU-resident op chaining (`runChain`).** Added `runChain(ops[], data, dims, params[])` to `src/services/WebGpuBackend.ts`. It ping-pongs between two persistent STORAGE buffers across all ops in a *single* command submission and reads back exactly **once** at the end (one `mapAsync`), eliminating the per-op `outputBuffer → readbackBuffer → mapAsync` round trip that `runOp()` pays for every op. Each op gets its own 16-byte uniform buffer (passes coexist in one submit, so they can't share a mutable uniform). `runOp()` is untouched for single-op callers.
  - **Pooled allocator:** adopted the size-bucketed (4KB) pool pattern from `WebGpuOscillator.ts` — `storagePool` (STORAGE|COPY_SRC|COPY_DST ping-pong pair) + `readPool` (MAP_READ|COPY_DST), capped at 4/bucket, plus a `destroy()` teardown. Replaces fresh per-call STORAGE allocations on the chain path. (Full unification of both classes onto one shared pool left as a follow-up — kept this change small.)
  - **Fallback + telemetry preserved:** returns `null` when WebGPU is unavailable (callers fall back to CPU, same contract as `runOp`); registers `webgpu-compute` resolution on init and `logEngineFallback(...)` on adapter/device/alloc/dispatch failure, mirroring the oscillator's telemetry. Stays OFFLINE/precompute — never awaited on the audio worklet thread.
  - **Call site:** `VoiceDesigner._runGpuChain()` + new `dspMangle()` (sharpen→quantize→tremolo) demonstrate composing ops with no intermediate readback; degrades to the existing per-op CPU fallback when GPU is down or `runChain` returns null.
  - **Tests:** `src/services/WebGpuBackend.test.ts` (6 cases) — empty/unavailable contracts, one pass per op + single readback, ping-pong wiring (op N output === op N+1 input), unknown-op error path, pool reuse across calls. Verified all 10 GPU tests green under vitest 2.1.9 (the repo's installed vitest 4.1.9 is incompatible with vite 5.4.21 — the documented red-CI mismatch above; full `tsc --noEmit` is clean: 0 errors).

## Ideas
_(No fresh user entries as of 2026-07-27 → New Idea mode. The 2 unpicked generated directions are parked below for future runs.)_
- [ ] **New Idea (07-27, unpicked #2) — Kick off RBS Epic #773 song-fidelity now that CI is green.** Start with the smallest independently-shippable child: `RbsExporter` write-path (#780) or v1.5 subset import (#778). Why-now: backlog has flagged this as "the next big theme after CI is green" for weeks, and CI is now green. Scope: multi-day (whole epic); ~full-day for one child issue. Decoupled from the 303-authenticity DSP work (touches `src/**/Rbs*`, not `emscripten/`/`AudioDSP`).
- [done — verified 2026-08-17] **Holographic-knob GPU render-loop consolidation + reduced-motion a11y fallback** (07-27 parked idea #3) — **ALREADY LANDED on `main`**. Verified: `src/components/knobMotion.ts` provides `getPrefersReducedMotion()`/`subscribeReducedMotion()` (live `matchMedia('(prefers-reduced-motion: reduce)')`, no-window guarded); `KnobGPUContext.ts` (959L) has one shared rAF loop with idle self-cancel, `useSyncExternalStore` subscribe/getSnapshot, a `device.lost` handler (L771), `visibilitychange` handling (L445), reduced-motion wiring (L467), and a single `createCommandEncoder` per frame (L897); `knobMaterial.ts` freezes the time uniform for idle/reduced-motion static frames; `MagicKnob.tsx` carries no per-instance rAF; contract tests exist under `__tests__/knobMaterial/` + `KnobGPUContext.scheduler.test.ts` + `.degradation.test.ts`. The 08-17 dispatch's drafted Copilot issue B (re-implement this) was **discarded before filing** — Copilot lane retargeted to owner issue **#1106**.
- [done — 2026-04-27] **Verify bug-report.md staleness** — confirmed stale; file deleted.
- [done — 2026-05-25] **Holographic knob GPU context unification** — `KnobGPUContext.ts` singleton + `MagicKnob.tsx` ported.
- [done — 2026-06-01] **Holographic knob WGSL/2D drift-kill** — `knobMaterial.ts` shared contract consumed by WGSL + Canvas2D; `knobMaterial.contract.test.ts` guards palette/geometry/bloom parity.

## Backlog
- [in progress — 2026-06-29] **Green-CI guardrail** — the lockfile half is moot (vitest pinned, lockfile consistent). The live half is the **missing `tsc` type gate in `ci.yml`** → today's FIX FIRST. Also still worth a `lockfile-check` / `pnpm install --frozen-lockfile` pre-push hook so a future stale lockfile fails before merge.
- [ ] **Green-CI guardrail (follow-up to today)** — add a CI step (or husky/pre-push) that runs `pnpm install --frozen-lockfile` locally so a stale lockfile fails before merge, not after. Consider a `lockfile-check` job.
- [ ] **vitest 4 migration (if pinned back today)** — if today's fix reverts vitest to 2.x, track the deliberate v4 upgrade as its own task (config/API surface, happy-dom compat).
- [done — 2026-07-27] **Green-CI guardrail — COMPLETE.** `.husky/pre-push` runs the full `install --frozen-lockfile && tsc -b && lint` gate; `main` green across 8 consecutive CI runs; `SingingVoice.ts` lint fixed via scoped disable; `useAudioEngine.ts` `as any` → 0. Standing item since 06-22, across 4 Fix First runs — closed. (See Done 2026-07-27.)
- [ ] **Phase-1 TB-303 offline render — Epic #972 / issue #974 (TODAY'S FOCUS, 2026-07-27).** OpenMP oversampling (`OVERSAMPLE_FACTOR` 1/2/4) in `emscripten/audio_dsp.cpp` + `Offline303Renderer.worker.ts` + `render303Offline()` API + telemetry. Offline/freeze/export only — real-time worklet untouched. Verify against Phase-0 baselines (`docs/audio-engine/303-authenticity-gaps.md`, `303-baseline-spectra/baseline_metrics.json`). Unblocks Phase-2 (#975 diode-ladder CPU) → Phase-3 (#976 WGSL 303 voice).
- [ ] **vitest 4 / vite 6 forward migration (deferred deliberately)** — `main` is pinned to `vitest 2.1.9` / `vite ^5.4.21`. Track the v4/v6 bump as its own task (vitest-4 `Reflect.construct` mock breakage, `vite/module-runner` export, `@fast-check/vitest` peer compat — all documented in the 2026-06-22 toolchain log above).
- [ ] **Drop unused `@babel/helper-plugin-utils`** from `package.json` devDeps (grep-confirmed unused in `src/`; accidental dep from #793). Tiny; fold into today's branch or a hygiene pass.
- [ ] **Epic #773 — ReBirth RBS v1.5/2.0 song fidelity** (opened 2026-06-18 by `cursor`). 8 child issues: #774 real-fixture corpus + golden suite, #775 DEVL packed-struct parser (`rbs.h`), #776 PCF + per-pattern 303 param extraction, #777 per-track TRAK controller tables, #778 v1.5 subset support, #779 32-pattern banks beyond 8 slots, #780 RbsExporter (write `.rbs`), #781 E2E import→song-mode→automation. This is the next big theme after CI is green.
- [ ] **Repo hygiene** — 13 root `*.md` (several stale: `plan2.md`, `test_plan.md`/`test_plan2.md`, `lfo_sync_plan.md`, `grok.md`, `copilot-session-*.md`, `agent_plan.md`, `claude.md`) + **18 root one-off `.py` scripts**, most of them prior throwaway brace/TS-error patchers: `fix_braces2.py`, `fix_missing_brace.py`, `fix_ts_errors[1-5].py`, `patch_engine*.py`, `patch_exact_manual17-20.py`. **These are archaeological evidence of earlier failed attempts to hand-patch the exact `useAudioEngine.ts` parse error that is today's Fix First** — do NOT run them against the file; kimi-cli fixes it properly, these get archived. `DOCS.md` already exists (populate it as the index) + archive scripts into `tools/archive/`. **= Today's decoupled Copilot issue (B), re-scoped 07-06 (original RBS test+docs scope is already satisfied — see Done).**
- [ ] Rubberband phoneme-aware time-stretch + `ExpressiveVoiceProcessor.ts` pending per `RUBBERBAND_ENHANCEMENT_PLAN.md`.
- ~~RBS import test+docs polish~~ → **DONE (verified 2026-07-06)**, see Done. Remaining RBS work is feature-sized (Epic #773 children #778/#779/#780 — v1.5 subset, 32-pattern banks, full TRAK song-mode export), not test/docs polish.

## Done
- 2026-07-27 — **Green-CI guardrail + full pre-push gate (2026-07-20 Fix First) — LANDED & VERIFIED; `main` GREEN.** The 07-20 lint Fix First fully executed: `.husky/pre-push` runs `pnpm install --frozen-lockfile && pnpm exec tsc -b && pnpm lint`; `package.json` gained `prepare: husky` + `husky@^9.1.7`; `src/engines/SingingVoice.ts` mixin lint fixed via scoped `eslint-disable` (idiom preserved); `src/hooks/useAudioEngine.ts` `as any` stripped 13→**0**. Verified live: 8 consecutive `ci.yml` runs on `main` = `success` (HEAD `2e3af39` / run #1220, 2026-07-27); `.swarm-state.md` confirms all 5 gate steps green + 1280 tests. **This closes the standing "Green-CI guardrail" backlog item carried since 06-22 across 4 Fix First runs** (lockfile → parse/type → lockfile → lint). The recurring "un-run gate reds `main`" meta-failure is structurally prevented. **First non-Fix-First planning run in 4+ weeks.** Also this week (CI-validated on the now-green gate): Phase-0 TB-303 authenticity baselines + gap doc (#973 → #1001/#1002), per-phoneme TTS vibrato (#998), auto-gen sampler rhythm from TTS alignment (#992), rubberband vowel-preserving time-stretch (#994), multiple Bolt perf/GC passes.
- 2026-07-20 — **Lockfile regen + first live gate pass (2026-07-13 Fix First) — LANDED (with one residual red).** Verified live on `origin/main` (`7bea600`): `@babel/helper-plugin-utils` removed from `package.json` **and** `pnpm-lock.yaml` regenerated → `pnpm install --frozen-lockfile` **passes**; the `build:wasm → tsc -b → lint → vitest` gate **runs on main for the first time**; **`tsc -b` passes** (the 62-commit merge spree's type drift hand-patched across #943–#947 + 3 `push fix` commits; `as any` cut 16→13). The 07-13 lockfile break is resolved. **Carried into 07-20 Fix First:** the now-un-masked `pnpm lint` step fails main (3 ESLint errors in `SingingVoice.ts`, + possible lint debt behind them); last 13 `as any` unstripped; the pre-push gate guardrail still doesn't exist (this is the break it would have prevented — 3rd in a row).
- 2026-07-13 — **CI type-safety hardening (2026-07-06 escalated Fix First) — LANDED (with one regression).** Verified live on `origin/main`: `ci.yml` gained a **`Type-check (tsc)` step** (`pnpm exec tsc -b`); **lint un-masked** (`continue-on-error: true` removed); **`debug_build.yml` deleted** (perma-red workflow resolved by removal); **`@babel/helper-plugin-utils` dropped from `package.json`**; `useAudioEngine.ts` parse issue resolved (file present, 1048 lines) and `as any` cut 21→16. The 06-06/06-29 planning-only cycle finally converted to landed change. **Carried into today's Fix First:** (1) the babel removal left `pnpm-lock.yaml` stale → install fails → the new gate has never run (regen lockfile); (2) the gate, once live, still needs its first real pass against the merge spree; (3) last 16 `as any` not yet stripped; (4) the `--frozen-lockfile` pre-push guardrail still doesn't exist (this is the break it would prevent).
- 2026-07-06 — **RBS importer/parser test+docs — VERIFIED ALREADY COMPLETE.** Reconciled the standing "RBS import test+docs polish" backlog item against actual repo state (was drafted as the decoupled Copilot issue): the harness is mature — **14 test files** (`RbsParser.property.test.ts` fuzz + structured roundtrips via fast-check, `RbsCorpus.test.ts` 7-fixture golden corpus, `RbsImporter.snapshot.test.ts`, `.edge`/`.boundaries`/`.fidelity`, `RbsExporter.test.ts` export→import roundtrip, DEVL/PCF/TRAK/V15) — and **`docs/audio-engine/RBS_IMPORT_PIPELINE.md`** (174 lines: pipeline table, error-code table, field-mapping highlights, testing strategy, export chunk table, Playwright E2E table). Three chat models (Gemini/Grok/Kimi) independently recommended exactly this shape (synthetic byte fixtures via factory, semantic assertions over golden snapshots, fast-check crash guards + structured roundtrips, `pool: forks`) — confirming the existing harness is modern and sound. Nothing to build; Copilot slot re-pointed to repo hygiene.
- 2026-06-29 — **Restore green CI on `main` (2026-06-22 Fix First) — LANDED & VERIFIED.** Fix merged via the conservative fork (vitest pinned `2.1.9`, lockfile regenerated, stray brace in `audioPlayback.ts` removed, `vocoderMix` added to property-change unions). `ci.yml` (Lint + Test) green and stable across the week's merges (#793/#814/#816/#817/#818). The week's previously-unvalidated feature spree (formant vocoder #793, granular jitter #791, drum-retrigger #792, spectral morph #785, spectral resynthesis #814) is now CI-validated. Follow-up cracks (missing `tsc` gate, perma-red `debug_build.yml`, `as any` band-aids) carried into today's Fix First.
- 2026-06-15 — **Issue #720 — Oscillator fallback audit CLOSED (completed)** by ford442. The prior Fix First lead task landed during the week; all 6 degrading engines addressed. (Routine primed it 2026-06-15; closed 13:50 same day.)
- 2026-06-22 — _Reconciled, landed-but-UNVALIDATED this week (CI red — validation pending today's fix):_ formant-preserving vocoder phase 1 (PR #793), granular position jitter (PR #791), drum-retrigger optimization (PR #792), spectral morph automation (PR #785), audio inner-loop param hoisting (#786), + accessibility/palette batch (ScaleSelector #788, AutomationLane ARIA #787, modal close buttons #782/#784, focus rings).
- 2026-06-15 — **Palette: aria-labels on oscillator variant buttons** (PR #760, merged — commits `6b0f753` / `9ca0e24`). Closes prior sprint target #3.
- 2026-06-15 — **Vocal Pitch Envelope** (PR #759, Jules): `pitchAttack`, `pitchDecay`, `pitchAmount` on `Note` / `SamplerParams`; `SingingVoice.ts` setters; `ExpressiveVoiceProcessor.ts` attack/decay envelope; `rubberband-processor.ts` per-step + global routing; UI knobs in `SamplerPanel` + per-step controls in `SamplerVoicePanel` / `SamplerPitchControls` / `ContextMenuNode`.
- 2026-06-15 — **Time-Stretch Envelope** wiring completed (PRs #756, #757): `timeStretchEnvDepth` per-step + global path through `SingingVoice` → rubberband worklet.
- 2026-06-15 — **Palette: standardize focus rings** (PR #758).
- 2026-06-01 — Holographic knob render-pass PLUMBING + shared `knobMaterial.ts` contract on `main`.
- 2026-06-01 — Backlog reconcile: PRs #506 & #507 confirmed closed-unmerged; GitHub issue count reset.
- 2026-05-25 — Holographic knob GPU context unification (PR #617 context).
- 2026-05-18 — jc303/Open303 WASM pipeline stabilized (PRs #569 + #572).
- 2026-05-18 — Live keyboard drum voices with MIDI pitch shifting (PR #571).
- 2026-05-18 — Vocal Harmony Parallel Bus (PR #568).
- 2026-05-18 — Palette: decorative UI hidden from screen readers (PR #567).
- 2026-05-11 — Gesture Controls: pinch/scroll zoom on sequencer (PR #513).
- 2026-05-11 — BottomBar Accessibility Enhancements (PR #511).
- 2026-05-04 — TTS per-bank cold-start preload + onnxruntime-web cache purge devtool.
- 2026-05-04 — SamplerPanel `<fieldset>` conversion (PR #483 + a11y passes).
- 2026-05-04 — Master Bus Compressor (PR #479), MainSequencer re-render optimization (PR #480).
- 2026-05-04 — Hybrid Audio Engine Fallback HUD: `EngineHUD.tsx` + `engineTelemetry` (Ctrl+Shift+E / `?hud=1`).
- 2026-04-27 — Custom Waveform LFO, aria-label passes, step-sequenced reverb types, AdvancedNoteSelector Escape, DrumMachine memoization.
- 2026-04-20 — Accessible ARIA switches (PR #456).
- 2026-04-19 — Custom Sample Slicing UI (PR #455), orphaned aria-describedby fix (PR #454).
- 2026-04-18 — AI auto-mix assistant (PR #453).
- 2026-04-17 — Keyboard accessibility for custom radio groups (PR #452).
- 2026-04-16 — Step-sequenced formant shifts (PR #451).
- 2026-04-15 — Global convolution reverb effect.
- 2026-04-14 — Palette: tabpanel roles (PR #450).
- 2026-04-13 — Auto-slice by transients (PR #444), aria-busy on processing buttons (PR #445).
- 2026-04-12 — AdvancedNoteSelector + ScaleSelector (PR #443).
- 2026-05-31 — consonantEmphasis TTS control (PR #693 merged).
- 2026-05-30 — RBS → Hyphon importer scaffolding (PRs #685/#686 merged).

## Claude Wins / Post-Run Notes
_(Running log — fill in at end of each weekly session.)_

| Date | Shipped | Notes |
|------|---------|-------|
| 2026-06-01 | (incomplete) | Holographic-knob drift-kill started; `knobMaterial.ts` contract landed but session ended before full verification. |
| 2026-06-16 | _(pending)_ | Prep: plan refreshed, tests green after `pnpm run build:wasm`, issue #720 primed. |
| 2026-06-22 | ✅ green CI restored | Fix First: main CI red since 06-21 (stale pnpm-lock.yaml — vitest 2→4 bump in #793 without lockfile regen). Fix landed via conservative fork (vitest pinned 2.1.9); `ci.yml` green & stable. |
| 2026-06-29 | _(pending)_ | Fix First: main CI green but **never type-checks** (`ci.yml` has no `tsc`); merge-spree type drift slipped past it (vocoder formant `TS2339`, patched late w/ `as any`). Plus `debug_build.yml` perma-red (malformed YAML). Plan = add `tsc` gate + fix workflow + strip `as any`. Noah notified. |
| 2026-07-27 | ✅ green `main` — streak broken | 07-20 lint Fix First **LANDED & verified**: `.husky/pre-push` full-gate hook installed, `SingingVoice` lint fixed, `as any`→0, 8 consecutive green CI runs on `main`. Green-CI guardrail (standing since 06-22) CLOSED. First **New Idea** run in 4+ weeks → Today's focus = Phase-1 TB-303 offline render (#974). Noah notified. |
| 2026-08-03 | ✅ E2E matrix green | Fix First: restored Playwright chromium/firefox/webkit (webServer block, WebKit autoplay flag, StartOverlay resume race, PulseAudio) + purged 5 `.orig` merge artifacts. Merged #1046/#1052; 84 passed per `.swarm-state.md`. |
| 2026-08-10 | ✅ #1075 landed (fail-fast + tiered CI) | Fix First: owner P0 #1075 executed — `source-integrity` fail-fast `tsc` job + tiered `lint`/`test-unit`/`test-integration` gates landed; Copilot lane shipped master ISP limiter + LUFS (#1095). Verified 08-17. |
| 2026-08-17 | ✅ `.wasm?init` unit-tier stub landed | **Fix First → new CI break from the tiering.** `main` red across the whole window (23 fail / 3 pass / 4 cancelled, 08-14→08-17). Cause: no-WASM `test-unit` tier can't resolve static `.wasm?init` imports (4 loaders; `src/wasm/*` gitignored + not built there); `source-integrity` `tsc` gate blind to Vite resolution. Plan = hermetic `*.wasm?init` stub in `vitest.unit.config.ts` + drive both test tiers clean + teach fast lane to catch unresolved bundler imports + require CI on `main`. Noah notified. |
| 2026-08-24 | _(pending)_ | **New Idea** — `main` green (08-17 `.wasm?init` stub landed via #1142; HEAD #1149/run #1522 `success`; ~9 trailing green runs). No user Ideas (chat tools down, 8th run); generated Ideas superseded by owner's 08-21 roadmap (#1134–#1140). Focus = **#1139 RBS song-mode fidelity** (kimi-cli main event); Copilot lane = shadow-playback-stack deletion slice of owner P0 #1134; Claude Code = whole-stack build→deploy dry-run→smoke. Noah notified. |
| 2026-08-24 | ✅ #1139 + #1134 slice landed | **New Idea** — both lanes shipped on 08-24: RBS song-mode TRAK export + `handleRbsImport` (#1154, `33a2b54`, 1596 tests green per `.swarm-state.md`) and the shadow-stack deletion + protobufjs override (#1155, `d4b6b59`). Verified 08-31. |
| 2026-08-31 | ✅ all 3 P0s landed | **Fix First** — SessionLaunchEngine accumulator fixed (`0608199`); `PcfEffect.ts` **and** `masterLoudnessStage.ts` converted to `?worker&url` with a lint guard + static test (#1177 class closed); export map populated, `check-release-dist.mjs` gate extended, export-name logging added (#1176). All via `28ec7d2`. Verified 09-07. **Note:** `.swarm-state.md` still shows P0-c open — stale, never updated after the final push. GitHub issues #1176/#1177 still open — owner action to close. |
| 2026-09-07 | _(pending)_ | **Fix First (2nd consecutive)** — **all three workflows dead at step 2.** `06ce5b2` (direct push to `main`) added `packageManager: pnpm@12.3.4` to `package.json` + a pnpm-12-written lockfile, colliding with 8 `pnpm/action-setup@v4 version: 9` pins → `Error: Multiple versions of pnpm specified`, every job fails in ~1 s (run #1656). Four merges landed on a dead gate since. Playwright E2E separately red 12/12 runs since ≥09-01, two failure shapes, cause not yet established. Plan = unpin `version: 9` ×8 → guard the collision in `source-integrity` + pre-push → root-cause and restore E2E → (owner) required branch-protection checks, 4th ask. Noah notified. |
| 2026-09-14 | _(pending)_ | **Fix First (3rd consecutive)** — 09-07 landed in full; `main` green (CI #1730, **E2E #1725 green**, 303 #497). Two dark guards are the new break: **#1236** `hyphon_native.wasm` won't instantiate (`LinkError` on `emscripten_date_now` / `_emscripten_get_now_is_monotonic`, missing from the hand-maintained `createEmscriptenEnv()` allowlist) → every 303/Prophecy voice on the JS fallback in production, while the guard test has always passed vacuously behind `if (!existsSync(wasmPath)) return;`. And **`Test Performance` red 34/34 runs since 2026-08-11, never green** — two `.wasm?init` resolution failures (the 08-17 unit-tier fix never reached `vitest.perf.config.ts`) plus `SamplerPanel.perf.test.tsx` hard-coded `49` vs 57 knobs; `perf-summary.json` has therefore never been produced. Plan = derive the import table from `WebAssembly.Module.imports()` + fail-loud native guard → port the wasm stub to the perf tier and hoist it into shared config → de-magic the render-count assertions (#1237 **Part A only**). Copilot lane = **#1238** dist budget + `manualChunks` + lazy modals. Noah notified. |
| 2026-08-31 | _(superseded by the row above)_ | **Fix First** — `main` HEAD **does not compile**: `SessionLaunchEngine.ts` `:181` declares `remaining` but `:187`/`:190` use `nextQueued` → TS2304 ×2 (run #1581) **and** a runtime `ReferenceError` in the audio-clock `tick()`. From ⚡ Bolt #1174, **merged on a `cancelled` CI run**. Window: 11 fail / 11 pass / 8 cancelled. Plus two owner production bugs filed 08-30, one family (asset resolution under `/hyphon/`): **#1177** raw-`.ts` worklet URL in `PcfEffect.ts:25` (Vite emits no such asset; `masterLoudnessStage.ts:21` likely the same, silently killing #1095's limiter/LUFS) and **#1176** empty `hyphon_wasm_export_map.json` on the deployed build → Open303/Prophecy fall back to JS. Plan = fix SessionLaunchEngine → `?worker&url` for all worklets + guard test → export-map delivery + `check-release-dist` assertion + log export names. Copilot lane = root-file hygiene (15 tracked junk files) + extend `check-root.mjs`. Noah notified. |

## Last run
Date: 2026-07-27
Mode: **New Idea** (first non-Fix-First run in 4+ weeks — the streak is broken)
Focus: Phase-1 of Epic #972 — OpenMP oversampling + offline 303 worker-pool (issue #974). Verified live on `origin/main` (HEAD `2e3af39`): last week's 07-20 lint Fix First fully LANDED — `.husky/pre-push` full-gate hook exists (`install --frozen-lockfile && tsc -b && lint`), `SingingVoice.ts` lint fixed via scoped disable, `useAudioEngine.ts` `as any` → 0, and the last 8 `ci.yml` runs on `main` are all `success` (run #1220 today). Foundation solid → no Fix First. `Ideas` had no fresh user entries → New Idea mode. Synthesized 3 directions; picked Phase-1 #974 (unblocked today by Phase-0 #973/#1001/#1002 landing its authenticity baselines + gap doc); parked RBS Epic #773 kickoff and holographic-knob render-loop consolidation in `Ideas`.
Outcome: Planning run. Reconciled: 07-20 lint Fix First → LANDED & VERIFIED (moved to Done; Green-CI guardrail backlog item — standing since 06-22 across 4 Fix First runs — CLOSED). weekly_plan.md updated (Today's focus = Phase-1 #974; CI toolchain log 07-27 reconcile; Done 07-27 entry; Backlog: guardrail done + Phase-1 active; Ideas: New Idea mode note + 2 parked directions; wins table 07-27 row; Last run). Dispatch: kimi-cli swarm = build the offline 303 render path per #974 (scoped to `emscripten/audio_dsp.cpp`, `emscripten/tests/`, `src/engines/AudioDSP.ts`, `src/workers/Offline303Renderer.worker.ts` [new], a new `src/audio/OfflineRenderer.ts`, `src/utils/engineTelemetry.ts`, `docs/audio-engine/` perf notes; **must not touch the real-time worklet or `useAudioEngine.ts`**), self-verifying against Phase-0 baselines with `.swarm-state.md` checkpoints; decoupled Copilot issue = holographic-knob GPU render-loop consolidation + reduced-motion a11y fallback (scoped to `src/components/Knob*`/`MagicKnob.tsx`/`KnobGPUContext.ts`/`knobMaterial.ts` — zero overlap with the audio-engine offline path); Claude Code = whole-stack build→deploy dry-run→smoke (audio init + WebGPU knob render); Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable in this environment (6th consecutive run) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions, and `Ideas` holds no fresh user entries so New Idea synthesis relied entirely on repo signal.

## Last run (archived) — 2026-06-29
Focus: Restore green CI on `main`. CI/Playwright/debug_build all failing since 2026-06-21 at the `pnpm install --frozen-lockfile` step — `pnpm-lock.yaml` is stale vs `package.json` (commit `2c36d10` / PR #793 bumped vitest `^2.1.3 → ^4.1.9` major + added `@babel/helper-plugin-utils` without regenerating the lockfile). Lint/Test/Build/E2E all SKIPPED.
Outcome: Planning run. #720 confirmed CLOSED. New epic #773 (RBS fidelity) reconciled into Backlog. Dispatch as logged. Chat-history tools unavailable — context from repo + GitHub MCP + plan file only.
| 2026-06-29 | _(pending)_ | Fix First: main CI green but **never type-checks** (`ci.yml` has no `tsc`); merge-spree type drift slipped past it (vocoder formant `TS2339`, patched late w/ `as any`). Plus `debug_build.yml` perma-red (malformed YAML). Plan = add `tsc` gate + fix workflow + strip `as any`. Noah notified. **NOTE (07-06): this plan was never executed — only the plan commit merged.** |
| 2026-07-06 | _(pending)_ | Fix First (escalated): last week's CI-hardening swarm never ran. Verified live — `useAudioEngine.ts` **does not parse** (esbuild `Expected ")"` @ L1013, confirmed by Jules PR #850); `ci.yml` still has no `tsc` + runs `lint` `continue-on-error:true`; `debug_build.yml` still perma-red (3/3 latest = failure); `as any` in the hook 4→21; babel dep still present. Plan = fix parse error → strip `as any` → add `tsc` gate → un-mask lint → fix `debug_build.yml` → drop babel dep. Noah notified. |

| 2026-07-13 | _(pending)_ | Fix First: last week's CI-hardening **landed** (`tsc` step added, lint un-masked, `debug_build.yml` deleted, babel dep dropped from package.json, parse error fixed). But dropping the babel dep without a lockfile regen left `pnpm-lock.yaml` stale → `ERR_PNPM_OUTDATED_LOCKFILE` → main red at install → tsc/lint/vitest all SKIPPED (gate never runs). 2nd lockfile break. Plan = regen lockfile → run the live gate → fix surfaced errors + last 16 `as any` → add `--frozen-lockfile` guardrail. Noah notified. **NOTE (07-20): lockfile regen LANDED — install + tsc now green; red moved to the lint step.** |
| 2026-07-20 | _(pending)_ | Fix First (4th): lockfile fix landed — `install --frozen-lockfile` + `tsc -b` now **pass** on main, gate runs end-to-end for the first time. But `main` **still red at `pnpm lint`** (3 ESLint errors in `SingingVoice.ts` L51/L53, mixin declaration-merge) — red 07-13→07-19, run #1097, **62 commits merged onto red main**. 3rd gate break running (lockfile→lockfile→lint); pre-push hook still absent. Plan = fix SingingVoice lint + any lint/tsc/test the spree added → strip last 13 `as any` → install a full pre-push gate hook. Noah notified. |

## Last run
Date: 2026-07-20
Mode: Fix First (4th consecutive)
Focus: Restore green `main` (now red at the lint step, not install) + finally install a pre-push gate hook. Verified live on `origin/main` (`7bea600`): last week's lockfile regen LANDED — `@babel/helper-plugin-utils` gone from `package.json`, `pnpm-lock.yaml` regenerated → `pnpm install --frozen-lockfile` passes; the `build:wasm → tsc -b → lint → vitest` gate runs on main for the first time; `tsc -b` passes (62-commit spree's type drift hand-patched across #943–#947 + 3 push-fix commits; `as any` 16→13). BUT the un-masked `pnpm lint` step now fails main — 3 ESLint errors in `src/engines/SingingVoice.ts` L51/L53 (`no-empty-object-type` + `no-unsafe-declaration-merging` ×2 on the intentional mixin declaration-merge). Run #1097 (29688357867, job 88196564266, 2026-07-19 13:09Z) = failure at lint; main red 07-13→07-19; 62 commits merged onto red main, none CI-validated. 3rd gate break running (lockfile→lockfile→lint); pre-push hook still absent. Primary task: (1) fix SingingVoice lint via scoped eslint-disable (intentional mixin idiom — don't delete it), (2) run the full gate locally to clean exit + fix any lint/tsc/test the spree added, (3) strip last 13 `as any` in useAudioEngine.ts, (4) install a `.husky/pre-push` full-gate hook (`install --frozen-lockfile && tsc -b && lint`), (5) verify both live workflows green before merge.
Outcome: Planning run. Reconciled: 07-13 lockfile Fix First → LANDED (install + tsc now green; moved to Done, lint red carried forward). weekly_plan.md updated (Today's focus = SingingVoice lint fix + full-gate pass + pre-push hook; CI toolchain log reconcile; Done entry; Backlog Green-CI guardrail → install/tsc green, lint + hook remain; wins table 07-20 row; Last run). Dispatch: kimi-cli swarm = fix SingingVoice lint + drive gate to clean exit + strip `as any` + add pre-push hook (scoped to `src/engines/SingingVoice.ts`, `src/hooks/useAudioEngine.ts`, `src/types.ts`, `.husky/`, `.github/workflows/`, `eslint.config.js`); decoupled Copilot issue = repo hygiene (root `*.md` + one-off `fix*/patch*.py` archival into `tools/archive/` — zero overlap with engine/lint/hook files); Claude Code = whole-stack build→deploy→smoke (after main is green). Jules wrap-up template provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable in this environment (5th consecutive run) — context from repo state + GitHub Actions MCP + plan file only; no visibility into Noah's in-week chat mentions or the `Ideas` section being empty of fresh entries.

## Last run (archived) — 2026-07-13
Date: 2026-07-13
Mode: Fix First
Focus: Restore green `main`, then install the lockfile guardrail. Last week's 07-06 escalated Fix First MOSTLY EXECUTED (first cycle to actually land): `ci.yml` gained a `Type-check (tsc)` step, lint un-masked (`continue-on-error` removed), `debug_build.yml` deleted, `@babel/helper-plugin-utils` dropped from `package.json`, `useAudioEngine.ts` parse error fixed, `as any` cut 21→16. BUT the babel removal wasn't paired with a lockfile regen → `pnpm-lock.yaml` still carries the orphan entry (5 refs) → `pnpm install --frozen-lockfile` fails with `ERR_PNPM_OUTDATED_LOCKFILE` on every main run (run 29243396503 / job 86794492928, 10:38Z); `build:wasm`, `tsc`, `lint`, `vitest` all SKIPPED. The new gate has never run on main, and #909–#921 merged onto a red/unvalidated `main`. 2nd lockfile-caused break (1st 06-22). Primary task: (1) regen `pnpm-lock.yaml`, (2) let the live gate run + fix surfaced tsc/lint/vitest errors + strip last 16 `as any`, (3) add a `--frozen-lockfile` pre-push/pre-merge guardrail, (4) verify install + tsc + build + test + both workflows green.
Outcome: Planning run. Reconciled: 07-06 escalated Fix First → LANDED (moved to Done with the one lockfile regression carried forward). weekly_plan.md updated (Today's focus = lockfile regen + live-gate pass + guardrail; CI toolchain log reconcile; Done entry; Backlog: Green-CI guardrail → type half landed, lockfile + guardrail + 16 `as any` remain; wins table 07-13 row; Last run). Dispatch: kimi-cli swarm = regen lockfile + run the now-live gate + fix surfaced errors + strip `as any` + add `--frozen-lockfile` guardrail (scoped to `pnpm-lock.yaml`, `package.json`, `.github/workflows/`, `.husky/`, `src/hooks/useAudioEngine.ts`, `src/types.ts`); decoupled Copilot issue = repo hygiene (root `*.md` + one-off `fix*/patch*.py` archival into `tools/archive/` — zero overlap with lockfile/workflows/audio hook); Claude Code = whole-stack build→deploy→smoke (after lockfile fix). Jules wrap-up template provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable in this environment (4th consecutive run) — context from repo state + GitHub Actions MCP + plan file only; no visibility into Noah's in-week chat mentions.

## Prep checklist (before each weekly Claude session)
- [ ] Refresh this file (date + 3–4 scoped targets)
- [ ] `pnpm install --frozen-lockfile && pnpm run build:wasm && pnpm run lint && pnpm test`
- [ ] `main` clean and pushed; optional branch `claude-YYYY-MM-DD` for PR-based output
- [ ] Prime the lead issue with reproduction notes
- [ ] Point Claude at: `docs/weekly_plan.md`, lead issue, latest feature merge, `AGENTS.md`

## Issue #720 repro notes (paste as issue comment if desired)
Diagnostics already on `main`: fallback transitions log via `logEngineFallback()` in `src/utils/engineTelemetry.ts` (console prefix `[EngineFallback]`). Runtime HUD: **Ctrl+Shift+E** or `?hud=1` (`EngineHUD.tsx`).

**Repro checklist (fresh checkout / CI-like env):**
1. `pnpm install --frozen-lockfile && pnpm run build:wasm && pnpm run build:emcc` — without WASM artifacts, engines fail at import/init.
2. Dev server must serve COOP/COEP headers (Vite config) for threaded `hyphon_native.wasm` / JC-303 pthread variants.
3. User gesture required to resume `AudioContext` before worklet engines initialize.

**Per-engine likely failure points (code audit, 2026-06-16):**
| Engine | Bridge | Common failure |
|--------|--------|----------------|
| Open303 / JC303 | `Open303Oscillator.ts`, `open303-processor` | `hyphon_native.wasm` fetch/timeout, worklet init error |
| Prophecy | `ProphecyOscillator.ts`, `prophecy-processor` | same native wasm + worklet path |
| Rust | `RustOscillator.ts` | `public/rust-wasm/` missing |
| WebGPU | `WebGpuOscillator.ts` | `navigator.gpu` null, device creation fail |
| Pyodide | `usePyodideEngine.ts` | CDN load / `window.Module` bootstrap |
| WASM (AS) | `WasmOscillator.ts` | `src/wasm/oscillators.wasm` not built |

## Context packet for 2026-06-16 run
| Resource | Why |
|----------|-----|
| `docs/weekly_plan.md` (this file) | Sprint scope + backlog state |
| [Issue #720](https://github.com/ford442/web_sequencer/issues/720) | Oscillator fallback audit — lead task |
| PR #759 merge (`8234a1e` / `c266138`) | Vocal pitch envelope — params + file map |
| `AGENTS.md` | Build/test/commit conventions, Four Worlds rule |
| [PR #760](https://github.com/ford442/web_sequencer/pull/760) | Open a11y follow-up (review or merge) |
| `src/utils/engineTelemetry.ts` + `EngineHUD.tsx` | Fallback diagnostics tooling already on `main` |

**Vocal pitch envelope file map (PR #759):**
- Types: `src/types.ts` (`pitchAttack`, `pitchDecay`, `pitchAmount` on `Note` + `SamplerParams`)
- Engine: `src/engines/SingingVoice.ts`, `src/engines/rubberband/ExpressiveVoiceProcessor.ts`
- Worklet: `src/audio-worklets/rubberband-processor.ts`
- UI: `src/components/SamplerPanel.tsx`, `SamplerVoicePanel.tsx`, `SamplerPitchControls.tsx`, `ContextMenuNode.tsx`
- Hook wiring: `src/hooks/useAudioEngine.ts`, `src/hooks/audioEngine/sampleManagement.ts`
**Fix First — stabilize the CI/test foundation before new feature work.**
Restore green Playwright E2E on `main` (#1036: WebKit launch flags + flaky `StartOverlay` gesture/autoplay race + missing `webServer` block in `playwright.config.ts`) and purge the 5 committed `.orig` merge artifacts polluting `src/` (#1039). No new features land on a red E2E matrix or a repo carrying merge scars.

Secondary (decoupled, for Copilot lane): master true-peak (ISP) limiter + LUFS/peak metering suite on the audio-graph output — touches `src/audio/graph/` + a new meter worklet, zero overlap with the test-fixture files kimi-cli will touch.

## Ideas
<!--
User-written ideas go here during the week. Routine prioritizes these over generated ideas.
Format: - [ ] Short description
This section was EMPTY at first run (no prior weekly_plan.md existed). The two unpicked
generated ideas below are seeded so future runs inherit them.
-->
_(2026-09-21: **FIX FIRST** run (4th consecutive) — `Test Performance` is red on 41/41 runs and has never been green, so no Idea is picked today. Still **no fresh user-written entries** (chat-history tools unavailable, **12th consecutive run**). The 09-14 note's suggested next picks (#1240 SMF import/export, #1241 PWA/service worker) are both resolved or rotated: **#1241's substance shipped** — `b748af9` (#1260) registered a real PWA service worker and moved project autosave to OPFS, with review findings fixed in `f1fa318` (#1261) — and **#1240 is now owner issue #1271**. The 09-11 board is fully closed; the live board is #1257 / #1264 / #1268–#1273. For the next non-Fix-First run the natural picks from the live board are **#1271** (Standard MIDI File import/export — greenfield, fully decoupled from the audio thread) and **#1270** (finish the `types.ts` split + the `pnpm.overrides` pnpm 12 silently ignores, which folds in a long-standing backlog item). #1138/#1136 below remain frozen provenance.)_

_(2026-08-24: **New Idea** run — `main` green, no Fix First. Still **no fresh user-written entries** (chat-history tools unavailable, 8th run). The two generated entries below are now **superseded by owner-filed issues** from the 08-21 roadmap wave: #1033 sampleRate → owner **#1136**; #1035 non-silent fallback → tracked under the owner engine epics. With the generated pool reconciled/exhausted, curated 3 directions from the owner roadmap; picked **#1139 RBS song-mode** (today's focus), parked the two below.)_
- [in progress — 2026-08-24] **RBS song-mode fidelity → owner issue #1139** (`feat(rbs): song-mode TRAK export + remaining import→engine wiring`). Today's focus / kimi-cli main event. The long-flagged "next big theme after CI is green," now owner-scoped.
_(2026-08-31: **FIX FIRST** run — `main` HEAD does not compile and two owner production bugs (#1176/#1177) are open, so no Idea is picked today. Still **no fresh user-written entries** (chat-history tools unavailable, 9th consecutive run). The two parked directions below stay parked and are inherited by the next non-Fix-First run.)_
_(2026-09-14: **FIX FIRST** run (3rd consecutive) — `main` is green but `hyphon_native.wasm` does not instantiate in production (#1236) and the `Test Performance` workflow has never been green in 34 runs, so no Idea is picked today. Still **no fresh user-written entries** (chat-history tools unavailable, **11th consecutive run**). The two parked directions below (#1138, #1136) are now **stale**: the owner closed that entire board on ~09-11 and replaced it with the #1234–#1241 wave, so future non-Fix-First runs should source Ideas from the new wave — the natural next picks there are **#1240** (Standard MIDI File import/export, fully decoupled greenfield) and **#1241** (register the shipped-but-inert service worker + OPFS project storage, same "shipped but dark" family as today's work). Leaving #1138/#1136 below for provenance only.)_
_(2026-09-07: **FIX FIRST** run — all three CI workflows are dead on every commit (pnpm `packageManager` vs `action-setup version: 9` collision) and Playwright E2E has been red for a week, so no Idea is picked today. Still **no fresh user-written entries** (chat-history tools unavailable, **10th consecutive run**). The two parked directions below (#1138, #1136) stay parked and are inherited by the next non-Fix-First run.)_
- [done — 2026-08-31] ~~**RBS song-mode fidelity → owner issue #1139**~~ — **LANDED** as #1154 (`33a2b54`, 08-24): song-mode TRAK export (`trakExport.ts` + multi-pattern `iffBuilder` + `RbsExporter`), `handleRbsImport` completed (`TODO(#651)` removed; automation / Open303 / trakEvents wired), >8-slot mapping warnings, v1.5 round-trip test, and both E2E paths green in `tests/rbs-automation.spec.ts`. GitHub issue #1139 is still **open** — owner action: close it, or state what remains.
- [ ] **Live high-fid 303 A/B + editable diode-ladder → owner issue #1138** (parked New-Idea direction #2, 2026-08-24). Real-time high-fidelity 303 with an A/B against a hardware TB-303 reference + a user-editable diode-ladder filter. Why-now: 303-authenticity is a standing focus area and Phase-0 baselines already exist; multi-day epic. Gated by owner P0 #1134 (do foundation first).
- [ ] **AudioContext `sampleRate` policy + output-device `setSinkId` → owner issue #1136** (parked New-Idea direction #3, 2026-08-24). Deterministic sampleRate negotiation + selectable output device. Why-now: foundation/stability focus area, subsumes the old generated #1033 idea; half-day/full-day. Decoupled from RBS + 303.
- [x] ~~Master true-peak (ISP) limiter + LUFS/short-term/momentary metering suite on the master bus~~ — **DONE 2026-08-17** (shipped as #1095, BS.1770-5 LUFS + ISP limiter on the master bus).
- [~] ~~`OscillatorBackend` interface + deterministic non-silent fallback (#1035)~~ — superseded 2026-08-24, tracked under owner engine epics.
- [~] ~~AudioContext `latencyHint`/`sampleRate` negotiation policy (#1033)~~ — superseded 2026-08-24 by owner **#1136** (parked above).

## Backlog
<!-- Unfinished items, known bugs, deferred work. Reconciled from open GitHub issues + repo state. -->
- [in progress — 2026-09-21] **P0: `Test Performance` red 41/41 runs, never green — TODAY'S FOCUS.** Confirmed on run **#41** (`35559956239`, 09-21T04:09Z, HEAD `ebcc802`): `audioExport.perf.test.ts` → `Failed to resolve import "../wasm/audioExport.wasm?init" from src/utils/audioExport.ts`, `useAudioEngine.perf.test.tsx` → same on `../wasm/trackFreezer.wasm?init`; 3 files / 33 tests pass. Cause is not the stub question the 09-14 run litigated: `docs/PERFORMANCE_BUDGET.md:282–292` correctly forbids stubbing the perf tier, and `test-perf.yml` correctly should not stub — but it also never runs `build:wasm`, and `src/wasm/*.wasm` is gitignored (`.gitignore:57`). Fix = build the five AssemblyScript targets (`oscillators`/`freezer`/`fft`/`audioexport`/`xmexport`, `asc` only — no Rust, no Emscripten) in the workflow, correct the doc's file list (it names `exportLoudness.perf.test.ts` + `wasmMigration.bench.test.ts`, neither of which fails and the latter imports no WASM), add nightly-failure visibility, and commit the first green `perf-summary.json` as the baseline.
- [ ] **Owner board rotated AGAIN — the #1234–#1241 wave is fully closed; the live board is now #1257 / #1264 / #1268–#1273 (filed 09-15 → 09-18).** Verified 2026-09-21 via the open-issues API; every backlog reference below to #1234–#1241 is provenance only. The live 8:
  - **#1257** [opus medium] perf(build): make `onnxruntime-web` genuinely lazy, then split and budget the entry chunk (successor to #1238; `dist-budget.json` now exists at root with `entryChunkMaxBytes: 1650000`, so part of that shipped).
  - **#1264** [opus low] Agent task: scan wiped positive changes + audit every issue for implementation completeness.
  - **#1268** [opus medium] foundation(osc): collapse duplicate wave engines — `CppOscillator` never landed; Rust/AS dump one-shot buffers.
  - **#1269** [opus medium] foundation(audio): delete AudioDSP/Open303Native shadow stack; AudioContext round-2 (`renderSizeHint`, recreate, dead sustain hook).
  - **#1270** [sonnet high] foundation(ts): finish the `types.ts` split, remaining live mega-modules, **and the `pnpm.overrides` that pnpm 12 ignores** — the long-standing backlog item below is now owner-tracked here.
  - **#1271** [sonnet high] feat(midi): Standard MIDI File import/export (successor to #1240).
  - **#1272** [opus high] epic(303): live A/B (L2) + editable diode-ladder coefficients (L3) (successor to #1138/#1272 lineage).
  - **#1273** [opus high] epic(voice): SIMD C++ rubberband FX + phoneme elasticity; freeze must match the singing track.
- [ ] **Root residue has grown, and the `check-root` gate still only polices `*.py` and `*.md`. = today's decoupled Copilot lane (B).** Verified tracked at root on 2026-09-21: 20 files carried since #1181 was filed 08-31 and never executed (805 KB + 2 KB `.webm`, 4 verification PNGs totalling ~590 KB, `test_output.txt` 101 KB, `build_log.txt`, `lint_output.txt`, `build_pid.txt`, `pr_body.txt`, `debug.txt`, `commit_message.txt`, `previous_changes.patch`, `test_perf.ts`, `git.sh`, `fix_tabindex.sh`, `svg-demo.html`, `verification_notes.txt`) **plus 7 new `patch_engine_hud_{1..7}.sh` added this week** and **`perf-summary.json` + `src/perf-summary.json` committed as build artifacts**. `scripts/check-root.mjs` checks root `*.py`, root `*.md`, DOCS.md link resolution and `*.orig`/`*.rej` — nothing else, which is why 8 new files walked in unchallenged. Fix = purge + move the gate to an explicit root allowlist.
- [x] **09-14 Fix First P0-a (#1236 native WASM import table) — LANDED 2026-09-21** via `ab64bd4` (#1256), an independent parallel implementation; the swarm reset onto it rather than push duplicate code. See Done.
- [~] **09-14 Fix First P0-b (red perf tier) — PARTIALLY landed, carried forward as today's P0.** `ea4f514` (#1259 Part A) fixed the `SamplerPanel.perf.test.tsx` render-count assertions; `e53457b` fixed `perf-summary.json` being empty on disk. The `.wasm?init` half was reverted, not fixed.
- [in progress — 2026-09-14] **P0-a: `hyphon_native.wasm` does not instantiate — owner bug #1236 (TODAY'S FOCUS #1).** `createEmscriptenEnv()` (`src/audio-worklets/hyphonNativeImports.ts:224`) builds the import object from a hand-maintained allowlist missing `emscripten_date_now` and `_emscripten_get_now_is_monotonic`; the binary declares 28 imports and instantiation dies with `LinkError: Import #14 "env" "emscripten_date_now": function import requires a callable`. Every native 303/Prophecy voice is on the JS fallback in production. Fix = derive the import table from `WebAssembly.Module.imports(module)` with warn-once typed no-op stubs, keep explicit impls as overrides, implement the two real symbols, and replace the vacuous `if (!existsSync(wasmPath)) return;` guard with an `HYPHON_REQUIRE_NATIVE=1` integration-tier assertion + a no-binary synthetic-module unit test. Third instance of the #1176 → #1177 → #1236 "shipped artifact silently doesn't load" family.
- [in progress — 2026-09-14] **P0-b: `Test Performance` red 34/34 runs since 2026-08-11 — never once green (TODAY'S FOCUS #2).** Three causes in run `34805038350`: (1) `audioExport.perf.test.ts` → `Failed to resolve import "../wasm/audioExport.wasm?init"`; (2) `useAudioEngine.perf.test.tsx` → same on `../wasm/trackFreezer.wasm?init`; (3) `SamplerPanel.perf.test.tsx` `toHaveBeenCalledTimes(49)` vs 57 (`:54`/`:72`/`:93`). (1)+(2) are the 08-17 unit-tier break recurring — `vitest.perf.config.ts` never received the `wasm-stub-resolve` `enforce:'pre'` plugin that `vitest.unit.config.ts` carries, and `test-perf.yml` runs no `build:wasm`. Fix = port the stub **and hoist it into `vitest.shared.ts`** so future tiers inherit it; rewrite the render-count assertions as relationships (= owner **#1237 Part A** only — Part B, the `useAppState` mega-context retirement, is a separate multi-PR epic). Consequence to close out: `Collect perf summary` / `Upload perf artifact` have been `skipped` on all 34 runs, so `perf-summary.json` has never been produced and **no perf baseline exists**.
- [ ] **The perf tier is not in any pre-merge gate.** `test-perf.yml` runs only on `schedule` / `workflow_dispatch` / a `perf`-labelled PR; `.husky/pre-push` runs `install --frozen-lockfile && check-pnpm-action-setup && tsc -b && lint` — **no tests at all**, and `ci.yml` does not run `test:perf`. That is why a workflow could stay red for five weeks unnoticed. Once green, decide deliberately whether the perf tier joins the pre-merge gate or stays nightly-with-alerting; a nightly nobody watches is the `debug_build.yml` perma-red lesson repeating.
- [ ] **Owner 09-11 roadmap wave (#1234–#1241)** — the current live board (all `ford442`, 2026-09-11); the prior board is fully closed:
  - **#1236** fix(wasm): derived import table + fail-loud native guard — **today's Fix First #1**.
  - **#1237** perf(state): retire the `useAppState` mega-context (835L hook → one context value → ~100 fields destructured in `App.tsx:43`) **+ unbreak the red perf gate**. **Part A = today's Fix First #2**; Part B is a 6-PR migration (UI modals → transport/mix → sampler banks → pattern edit → instrument state → session/song) onto the existing `src/stores/` `useSyncExternalStore` slice pattern.
  - **#1238** perf(build): 2.75 MB entry chunk + 3.5 MB raster art ship unsplit and ungated — `manualChunks`, lazy modals, enforced dist budget. **= today's decoupled Copilot lane (B).**
  - **#1239** chore(lint): retire `legacyRelaxedRules` — `react-hooks/rules-of-hooks` is globally off; 31 findings is the whole cost of turning it back on.
  - **#1240** feat(midi): Standard MIDI File import/export — the sequencer has MIDI clock but no file interchange.
  - **#1241** feat(pwa): the service worker ships but is never registered; autosave silently drops samples — finish offline-first with OPFS project storage. (Same "shipped but inert" family as #1236 — worth pairing in a future run.)
  - **#1234** epic(drums): analog 808/909 kit as a C++ AudioWorklet. **#1235** epic(export): freeze/export compile the live graph.
- [x] **P0-a: every workflow fails ~1 s in — pnpm version collision — FIXED 2026-09-07 in PR #1213.** `package.json:108` declares `packageManager: pnpm@12.3.4+sha512…` (added by `06ce5b2`, a direct push to `main`) while 9 `pnpm/action-setup@v4` call sites across 4 workflow files pinned `version: 9`. v4 hard-errors: `Multiple versions of pnpm specified` (run #1656 / `34099099113`). All 9 pins removed so the action reads `packageManager`; `packageManager` deliberately kept, since the committed lockfile was written by pnpm 12 and carries `configDependencies`/`packageManagerDependencies`. Verified locally under pnpm 12.3.4: `--frozen-lockfile` resolves clean, 453 entries pass supply-chain policy, all 4 workflows still parse.
- [ ] **`pnpm.overrides` in `package.json` is silently dead under pnpm 12 — STILL OPEN, verified 2026-09-14** (`package.json` still carries `"pnpm": {"overrides": {"protobufjs": "^7.5.8"}}`). Same "control is switched off and nobody can tell" family as today's two P0s; worth folding into the next lockfile-touching change rather than a standalone PR. pnpm 12 warns `The "pnpm" field in package.json is no longer read by pnpm. The following keys were ignored: "pnpm.overrides"` — so the `protobufjs: ^7.5.8` floor from the #1134 slice (#1155) is no longer enforced. No current exposure (the lockfile resolves `protobufjs@7.6.5`, which satisfies it), but the control is off. Fix = move it to `overrides:` in `pnpm-workspace.yaml`. **Requires a lockfile regen** (`ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` otherwise), so it was deliberately left out of PR #1213 — do it with CI green and validate the regen, given that an unvalidated lockfile edit is exactly what caused today's break.
- [in progress — 2026-09-07] **P0-b: guard the collision class (TODAY'S FOCUS #2).** A check in the `source-integrity` job and in `.husky/pre-push` that fails when any workflow pins `pnpm/action-setup` `version:` while `package.json` sets `packageManager`. Same family (manifest/lockfile edit landing unvalidated) as the 06-22 and 07-13 breaks.
- [in progress — 2026-09-07] **P0-c: Playwright E2E red on 12/12 `main` runs since ≥09-01 (TODAY'S FOCUS #3).** Two distinct failure shapes — fast ~90 s (#1590/#1591/#1593/#1595/#1597) and full ~90 min with a 541 MB report upload (#1583/#1588/#1605/#1607/#1610/#1624/#1630/#1641). **Cause not yet established** — diagnose from run logs/artifacts before changing anything; likely two separate problems. Last recorded green = the 08-03 fix (#1046/#1052).
- [ ] **#1204 refactor: split `src/audio-worklets/open303-processor.ts` (1076L, not 1054) into modules <700L** — owner-filed 09-07 as a thin auto-audit stub; **body expanded 09-07** with the real structure and the bundling question settled empirically. **= today's decoupled Copilot lane (B).** Key finding: Vite's `?worker&url` pipeline emits **one self-contained chunk per worklet** — a throwaway build of the three worklets produced `open303-processor-CNKgXrnG.js` (36.14 kB), `rubberband-processor-Clw3moD4.js` (94.65 kB), `master-loudness-processor-BNZ1Vs7E.js` (13.92 kB), with **zero** runtime `import`/`export` statements in the emitted open303 chunk and the `/hyphon/` base correctly applied. The pattern already ships: all three worklets import 5 sibling modules each. So a source split is safe and needs no build changes. Corrections to note: `worker: { format: 'es' }` is already set (`vitest.shared.ts:18`), COOP/COEP/CORP are already set in `vite.config.ts`, and this repo is on **Vite 5.4.21**, not Vite 8.
- [ ] **`scripts/check-release-dist.mjs` does not assert worklet chunk self-containment** — it greps the *concatenated* dist JS for processor names (`REQUIRED_WORKLET_PROCESSORS`, :83–110) and rejects raw `.ts` in `addModule()`, but never checks that each worklet is one chunk with no runtime `import`/`export`. That invariant is what every worklet refactor depends on and it is currently unenforced. Folded into #1204's scope; worth having regardless.
- [ ] **#1203 refactor: split `src/audio-worklets/rubberband-processor.ts` (1273L) into modules <700L** — owner-filed 09-07. Higher collision risk than #1204: Jules/Bolt touch this file most weeks (#1186/#1188/#1196/#1198/#1205/#1208 in the last week alone). Sequence it after #1204.
- [ ] **#1181 chore(repo): purge 15 tracked root residue files + widen the `check-root` gate past `*.py`/`*.md`** — filed 08-31 as that week's Copilot lane; **not executed.** All 15 files still tracked at root (805 KB `.webm`, 214 KB + 36 KB verification PNGs, 101 KB `test_output.txt`, `fix_components.patch`, `previous_changes.patch`, `build_log.txt`, `lint_output.txt`, `build_pid.txt`, `pr_body.txt`, `verification_notes.txt`, `test_perf.ts`, `git.sh`, `fix_tabindex.sh`, `svg-demo.html`). Cheap second Copilot lane whenever there's capacity.
- [ ] **Owner action — close shipped-but-open issues.** #1176 and #1177 both **fixed and landed** in `28ec7d2` (09-01) but still open; #1139, #1152, #1153 likewise carried from 08-31. Nothing to build.
- [ ] **`.swarm-state.md` is stale** — Checkpoint 2 still lists P0-c open, though it landed in the same push. Either have the swarm write its final checkpoint before pushing, or treat the file as advisory only.
- [in progress — 2026-08-24] **#1139 RBS song-mode fidelity — TODAY'S FOCUS.** Song-mode TRAK export (`RbsExporter.ts:45` stops warn-and-downgrading `mode:'song'`) + finish `handleRbsImport` (`useSongStorage.ts:557` `TODO(#651)` — apply tb303*Params/PCF to `Open303Manager`, convert `HyphonAutomationLane[]` into the live automation store, show `ImportReport` in `RbsImportModal`) + 32→8 slot mapping (no silent chain-step drops) + one green Playwright path (`tests/rbs-automation.spec.ts`). Verify via export→import round-trip goldens.
- [in progress — 2026-08-31] **P0-a: `main` does not compile — `SessionLaunchEngine.ts` `nextQueued`/`remaining` mismatch (TODAY'S FOCUS #1).** `:181` declares `remaining`, `:187`/`:190` use `nextQueued` → TS2304 ×2 in the `Source Integrity` fail-fast job (run #1581) **and** a runtime `ReferenceError` in `tick()` whenever a queued launch event is not yet due. From ⚡ Bolt #1174 (`bcabfff`), merged on a **cancelled** CI run. Fix + a regression test that `tick()` retains not-yet-due events.
- [in progress — 2026-08-31] **P0-b: owner bug #1177 — AudioWorklet modules 404 under the `/hyphon/` base (TODAY'S FOCUS #2).** `PcfEffect.ts:25` uses a raw `.ts` `new URL(..., import.meta.url)` that Vite never emits as an asset; convert to the `?worker&url` form used by `InternalClockAdapter.ts:8` / `singing-voice/workletSetup.ts:1`. **Verify `audio/loudness/masterLoudnessStage.ts:21`** — same pattern, would mean the #1095 master ISP limiter + LUFS metering has been dead in production since 08-17. Close the class with a guard test/lint rule against raw-`.ts` `addModule` URLs + a `dist/` emitted-asset assertion.
- [in progress — 2026-08-31] **P0-c: owner bug #1176 — Open303 + Prophecy degrade to JS on the deployed build (TODAY'S FOCUS #3).** `hyphon_native.wasm` instantiates but `loadHyphonWasmExportMap()` is empty at `/hyphon/`, so minified `open303_*`/`prophecy_*` exports can't be resolved. Determine whether `hyphon_wasm_export_map.json` is emitted into `dist/` and fetched at the right base; fix; extend `scripts/check-release-dist.mjs` to fail a release build on a missing/empty map; log actual export names on mismatch (owner's ask).
- [ ] **Owner action (standing since #1075, 08-08 — 3rd instance): make the `ci.yml` jobs required branch-protection checks on `main`.** #1174 merged with CI run #1576 `cancelled` and red-ed `main` for two days. Not fixable in code.
- [x] ~~**#1139 RBS song-mode fidelity**~~ — **DONE 2026-08-31** (merged #1154 / `33a2b54`; see Done). Song-mode TRAK export (`RbsExporter.ts:45` stops warn-and-downgrading `mode:'song'`) + finish `handleRbsImport` (`useSongStorage.ts:557` `TODO(#651)` — apply tb303*Params/PCF to `Open303Manager`, convert `HyphonAutomationLane[]` into the live automation store, show `ImportReport` in `RbsImportModal`) + 32→8 slot mapping (no silent chain-step drops) + one green Playwright path (`tests/rbs-automation.spec.ts`). Verify via export→import round-trip goldens.
- [ ] **Owner 08-21 roadmap wave (#1134–#1140)** — the current live board (all `ford442`, 2026-08-21):
  - **#1134 foundation(ts) — P0, "do before feature epics".** Delete shadow playback stacks (`hooks/audioEngine/samplerPlayback.ts` [973L, unused], `audio/playback/{synth,drum,sampler}Playback.ts` + `index.ts` [unused]) + import-graph guard test; enable `no-floating-promises`+`no-unsafe-assignment` on `src/hooks/**`; split `MainSequencer.tsx` (964)/`HardwareModule.tsx` (930) under ~700L; decide `LatencyCompensator.ts` (wire or delete); `pnpm.overrides` protobufjs `>=7.5.8`. **The shadow-stack-deletion + protobufjs slice = today's decoupled Copilot lane (B)** (zero overlap with the RBS files).
  - #1135 foundation(wasm): AS MVP dual-build, pinned wasm-opt, split Rubber Band from `hyphon_native`.
  - #1136 foundation(audio): AudioContext sampleRate policy + `setSinkId` (parked New-Idea #3).
  - #1137 epic(plugins): WAM2 Phase B (gated by #1134).
  - #1138 epic(303): live high-fid A/B + editable diode-ladder + hardware reference (parked New-Idea #2; gated by #1134).
  - #1139 feat(rbs): song-mode TRAK export (today's focus, above).
  - #1140 epic(voice): concatenative hybrid vocals, MFA alignment, HiFi-GAN vocoder.
- [ ] **No-WASM `test-unit` tier can't resolve `.wasm?init` — DONE 2026-08-24 (see Done).** ~~TODAY'S FOCUS (2026-08-17), P0.~~ `ci.yml`'s `test-unit` job fails at Vite `import-analysis` on the 4 top-level `.wasm?init` importers (`fftLoader.ts`/`WasmOscillator.ts`/`audioExport.ts`/`xmExport.ts`) because `src/wasm/*.wasm` is gitignored + not built in that no-WASM job. Fix = hermetic `*.wasm?init` stub alias in `vitest.unit.config.ts`; drive `test:unit` + `test:integration` + `lint` + `tsc -b` clean; make the fast lane catch unresolved bundler imports; require CI on `main` (branch protection — owner action).
- [x] ~~**#1075 fix(ci): restore parse/build/test integrity on main — P0 (2026-08-10).**~~ **DONE 2026-08-17** — `source-integrity` fail-fast `tsc -b` job leads `ci.yml`, gating tiered `lint`/`test-unit`/`test-integration`. The 4 flagged parse files, `SynthModulationEffects.tsx` guard, and export-map split all reconciled clean. **Remaining owner action:** make `Source Integrity` + the test jobs *required* branch-protection checks on `main` (can't be done in code).
- [x] ~~#1036 ci(e2e): restore green Playwright on main~~ — **DONE 2026-08-03** (merged #1046 + knob-click follow-up #1052; matrix 84 passed per `.swarm-state.md`).
- [x] ~~#1039 chore(repo): purge `.orig` merge artifacts~~ — **DONE 2026-08-03** (`.orig` gone from `src/`; root `weekly_plan.md` removed). Mega-module splits (#1030/#1031/#1032, all <700-line refactors) deferred → remain in backlog below.
- [ ] #1038 epic(graph): visual modular patch bay for sends, returns, and engine routing
- [ ] #1037 feat(audio): master true-peak limiter + LUFS/peak metering suite (overlaps today's Copilot lane)
- [ ] #1035 foundation(engine): OscillatorBackend interface + deterministic non-silent fallback
- [ ] #1034 foundation(wasm): audit emcc INITIAL_MEMORY=512mb, pthread pool, AS wasm-gc flags
- [ ] #1033 foundation(audio): AudioContext latencyHint + sampleRate negotiation policy
- [ ] #1032 refactor: split `src/hooks/useAudioEngine.ts` into modules <700 lines
- [ ] #1031 refactor: split `src/hooks/audioEngine/audioPlayback.ts` into modules <700 lines
- [ ] #1030 refactor: split `src/components/__tests__/knobMaterial.contract.test.ts` into modules <700 lines
- [ ] Follow-ups from Unreleased CHANGELOG: #632 (per-voice 303 engine selection UI), #633 (Prophecy controls discoverability), #634 (engine/help visibility)

## Done
<!-- Completed items, archived with date. -->
- [x] **#1236 `hyphon_native.wasm` derived import table (09-14 Fix First P0-a) — LANDED & VERIFIED 2026-09-21.** Merged as `ab64bd4` (#1256). `installDerivedFunctionStubs()` in `src/audio-worklets/hyphonNativeImports.ts` walks `WebAssembly.Module.imports()` and auto-stubs unknown symbols with one warning each, keeping explicit implementations as overrides; `emscripten_date_now` and `_emscripten_get_now_is_monotonic` are implemented for real; `src/__tests__/hyphonNativeImports.test.ts` runs hermetically in `test:unit` **and** fails loud in `test:integration` via `vitest.setup.integration.ts` forcing `HYPHON_REQUIRE_NATIVE ??= '1'`. Native 303/Prophecy voices instantiate again instead of silently degrading to JS. Landed independently of the 09-14 swarm, which discovered the overlap mid-run and reset onto it (`.swarm-state.md`, "superseded-work reconciliation") — worth noting as a coordination cost, not a failure.
- [x] **`perf-summary.json` was empty on disk even on a green `test:perf` run — FIXED 2026-09-15** (`e53457b`). Two independent causes: `scripts/collect-vitest-perf.mjs` read `VITEST_PERF_SUMMARY` from its own process env, which `vitest.setup.perf.ts` only ever sets inside the vitest worker, so the collector silently overwrote real output with an empty stub (now defaults the handoff path to its own output path); and `vitest.setup.perf.ts`'s `afterAll` wrote fresh per test **file**, so each file clobbered the previous one's samples (now read-merges, with a `globalThis`-keyed symbol clearing stale files once per run). Verified locally at 28 real samples. **Still invisible in CI** — the tier never gets far enough to reach the collect step, which is today's focus.
- [x] **09-07 Fix First (pnpm collision + collision guard + Playwright E2E) — LANDED & VERIFIED 2026-09-14; `main` GREEN.** All three parts shipped. **P0-a:** every `pnpm/action-setup@v4` `version:` pin removed across the 4 workflow files so the action reads `packageManager` (PR #1213); `packageManager: pnpm@12.3.4+sha512…` deliberately retained at `package.json:108` because the committed lockfile is pnpm-12-shaped. **P0-b:** the collision class is now guarded — `scripts/check-pnpm-action-setup.mjs` exists and `.husky/pre-push` runs `pnpm install --frozen-lockfile && node scripts/check-pnpm-action-setup.mjs && pnpm exec tsc -b && pnpm lint`. **P0-c:** **Playwright E2E is green** — run **#1725** on HEAD `0a0bcc7` `success` in 22 min, ending the 12/12 red streak that ran from ≥09-01; the `.swarm-state.md` Checkpoint 3 findings (GHA-killed preview → Vite dev → `hyphon_native.js` 404; `document.baseURI` loading; WebKit pthread worklet stubs; `workers: 1`; the `isAppleWebKit()` UA over-match that broke happy-dom unit tests) all landed. CI #1730 and 303 Quality #497 also `success` on the same HEAD. **Owner action completed:** the entire prior issue board was closed (#1134–#1140, #1176/#1177, #1181, #1203/#1204, #1030–#1039, #1139/#1152/#1153) and replaced by the 09-11 wave #1234–#1241 — the "close shipped-but-open issues" backlog item is cleared. **Still open (5th instance):** required branch-protection checks on `main`.
- [x] **08-31 three-part Fix First (P0-a/P0-b/P0-c) — LANDED & VERIFIED 2026-09-07.** Merged as `0608199` + `28ec7d2` (direct pushes, 09-01). **P0-a:** `SessionLaunchEngine.ts:181` declares `nextQueued`, agreeing with `:187`/`:190`; `src/session/__tests__/sessionLauncher.test.ts` gained the not-yet-due-retention regression case (+25L). `main` compiles again. **P0-b (#1177):** `PcfEffect.ts:25` **and** `masterLoudnessStage.ts:10` converted to `?worker&url` — the latent second instance was real, so the #1095 master ISP limiter + BS.1770-5 LUFS metering had been silently dead in production since 08-17 and is now live. Class closed with `src/__tests__/audioWorkletModuleUrls.test.ts` (49L, static guard), an `eslint.config.js` `no-restricted-syntax` rule against raw-`.ts` `addModule` literals, and `dist/`-emitted-asset assertions in `scripts/check-release-dist.mjs` (6 required worklet processors: clock, open303, prophecy, sustain, rubberband, master-loudness). **P0-c (#1176):** `public/hyphon_wasm_export_map.json` populated (+58L); `check-release-dist.mjs:119–155` now fails a release build when the map is missing, has zero entries, or is a stale identity map; `hyphonNativeImports.ts` +51L; the owner-requested export-name diagnostic logging added to `Open303Oscillator.ts` and `ProphecyOscillator.ts` (+45L each). **Owner action: GitHub issues #1176 and #1177 are still open — close them.** **Note:** `.swarm-state.md` Checkpoint 2 still lists P0-c as open; it is stale, not accurate.
- [x] **#1139 RBS song-mode fidelity (08-24 New-Idea focus, kimi-cli lane) — LANDED & VERIFIED 2026-08-31.** Merged as **#1154** (`33a2b54`, 08-24). Song-mode TRAK export via new `src/importers/rbs/trakExport.ts` + multi-pattern `iffBuilder` + `RbsExporter` (no more warn-and-downgrade of `mode:'song'`); `handleRbsImport` completed in `useSongStorage.ts` — `TODO(#651)` removed, automation lanes / Open303 init params / trakEvents wired; >8-slot mapping warnings so chain steps aren't silently dropped; v1.5 export round-trip test in `RbsExporter.test.ts`; both E2E paths green in `tests/rbs-automation.spec.ts` (chromium). `.swarm-state.md` Checkpoint 1 records `tsc -b`, `lint`, `test:unit` (1596), `test:integration`, and the Playwright spec all PASS. **Owner action: GitHub issue #1139 is still open — close it or state what remains.**
- [x] **Shadow-playback-stack deletion + protobufjs override (08-24 Copilot lane, slice of owner P0 #1134) — LANDED 2026-08-31.** Merged as **#1155** (`d4b6b59`, 08-24): dead shadow playback/timing stacks deleted, hooks type-safety ratcheted, oversized modules split, `pnpm.overrides.protobufjs ^7.5.8` verified live at `package.json:104`. This satisfies the owner's "delete shadow stacks before feature epics" gate. **Note:** duplicate owner issues **#1152** and **#1153** still describe this slice as open — owner action: close both.
- [x] **No-WASM `test-unit` tier `.wasm?init` resolution (08-17 Fix First) — LANDED & VERIFIED 2026-08-24.** `src/test/wasmInitStub.ts` (hermetic `WebAssembly.Instance` mock, default export `init()`) + the `wasm-stub-resolve` `enforce:'pre'` Vite plugin in `vitest.unit.config.ts` resolve `*.wasm?init` and bare `*.wasm` to the stub, so all 4 top-level importers (and future ones) resolve in the no-WASM unit tier without building binaries. Merged via #1142 (run #1507 `success`); `main` green since — HEAD #1149 = run #1522 `success`. **Carried forward (owner action, still open):** make the `ci.yml` jobs *required* branch-protection checks on `main` — the 08-18/19 single-commit reds landed directly on `main`, proving bot/direct pushes still bypass the gate.
- [x] **P0 #1075 CI parse/build/test integrity (08-10 Fix First) — LANDED & VERIFIED 2026-08-17.** `ci.yml` now leads with a fail-fast `source-integrity` job (`tsc -b --pretty false`, no WASM) that gates `lint` / `test-unit` (no-WASM, hermetic fetch guard) / `test-integration` (native `build:native:changed` + `check:exports` + offline-303 host g++). The flagged parse/merge files, `SynthModulationEffects.tsx` undefined-guard, unique `TB303_MODELS` ids, and release/debug export-map split are all clean at HEAD. **Carried forward:** (1) the tiered restructure introduced a **new** break — the no-WASM unit tier can't resolve static `.wasm?init` imports (today's Fix First); (2) still-open owner action — make the CI jobs *required* branch-protection checks on `main`.
- [x] **Master true-peak (ISP) limiter + BS.1770-5 LUFS metering (08-10 Copilot lane) — LANDED 2026-08-17** (#1095, master-bus ISP limiter + LUFS/momentary/short-term metering). The 08-03-generated Idea is retired.
- [x] **E2E Playwright matrix restored green + `.orig` purge (08-03 Fix First) — LANDED & VERIFIED 2026-08-10.** Merged #1046 (`webServer`/`reuseExistingServer` in `playwright.config.ts`, WebKit-only autoplay flag, StartOverlay `?e2e=1` non-blocking resume, PulseAudio global-setup) + follow-up #1052 (knob canvas no longer swallows module clicks; E2E uses real clicks). `.orig` merge artifacts removed from `src/`; duplicate root `weekly_plan.md` removed so `check:root` passes. `.swarm-state.md`: full matrix `--repeat-each=2` = 84 passed / 18 skipped, `tsc -b` + lint green.
- [x] CI unit gate restored green — 5/5 steps (install / build:wasm / tsc / lint / test = 1280 tests) — completed 2026-07-20 (per `.swarm-state.md`); pre-push husky guard added. NOTE: this covered the **unit/lint** gate, not the E2E/Playwright matrix, which remains red (see #1036).

## Last run
Date: 2026-09-21
Mode: **Fix First** (4th consecutive)
Focus: **Give the perf tier the WASM it has always required.** Reconciled first: 09-14's P0-a (#1236) **LANDED** via `ab64bd4`/#1256 — an independently-written parallel fix the swarm reset onto rather than duplicate — and the `perf-summary.json` write path landed via `e53457b`. Both moved to Done. `main` is green on HEAD `07a271f` for CI (#1811), Playwright E2E (#1806) and 303 Quality (#578). **But `Test Performance` is red on run 41 of 41 and has never once been green** (`35559956239`, 09-21T04:09Z): `audioExport.perf.test.ts` and `useAudioEngine.perf.test.tsx` still fail at `vite:import-analysis` on `.wasm?init`, the identical text as run #34 six weeks ago. The 09-14 swarm wrote a fix and then **reverted it**, correctly deferring to `docs/PERFORMANCE_BUDGET.md:282`'s "the perf tier intentionally does not stub WASM" — without noticing that `test-perf.yml` also never *builds* the WASM that policy therefore requires, and that `src/wasm/*.wasm` is gitignored. Both halves of the contradiction are true; the resolution is a build step. All five `.wasm?init` importers are AssemblyScript and `assemblyscript` is already a devDependency, so the fix costs one workflow step and no new toolchain. Copilot lane (B) filed as root-residue purge + `check-root` allowlist — 27 tracked root files and 2 committed `perf-summary.json` build artifacts, fully decoupled from the perf files.
Outcome: Planning run. Reconciled 09-14 → half landed (P0-a Done, P0-b carried). Owner board rotated a second time: #1234–#1241 fully closed, live board is now #1257 / #1264 / #1268–#1273 (8 open). Context gap: no chat-history tools in this environment (**12th consecutive run**), so Ideas remain sourced from repo + issue state only.


## Last run (archived) — 2026-09-14
Date: 2026-09-14
Mode: **Fix First** (3rd consecutive)
Focus: **Make two dark guards fail loud.** Reconciled first: the 09-07 Fix First **fully LANDED** and `main` is genuinely green — CI run **#1730**, **Playwright E2E run #1725 `success`** (22 min, ending the 12/12 red streak), 303 Quality #497, all on HEAD `0a0bcc7`. PR #1213 removed every `pnpm/action-setup@v4` `version:` pin; `scripts/check-pnpm-action-setup.mjs` exists and is wired into `.husky/pre-push`; the owner closed the entire prior issue board and filed the #1234–#1241 wave on 09-11. **But two breaks are live, both the same shape — a guard merged and inert ever since.** (1) **#1236:** `createEmscriptenEnv()` at `hyphonNativeImports.ts:224` builds the WASM import object from a hand-maintained allowlist missing `emscripten_date_now` and `_emscripten_get_now_is_monotonic`, so `hyphon_native.wasm` fails to instantiate (`LinkError: Import #14 "env" "emscripten_date_now"`) and **every native 303/Prophecy voice is on the JS fallback in production**; the guard test that exists to catch exactly this opens with `if (!existsSync(wasmPath)) return;` and `public/*.wasm` is gitignored, so it has always passed vacuously. Third instance of the #1176 → #1177 → #1236 family. (2) **`Test Performance` has been red on 34 of 34 runs since it was created 2026-08-11 — never once green** (owner saw only the 09-11 slice in #1237). Run `34805038350`: `audioExport.perf.test.ts` and `useAudioEngine.perf.test.tsx` both die at `vite:import-analysis` on `*.wasm?init` — **the 08-17 unit-tier break recurring, because `vitest.perf.config.ts` never got the `wasm-stub-resolve` plugin `vitest.unit.config.ts` carries** — plus `SamplerPanel.perf.test.tsx` asserting a hard-coded 49 knobs against 57 actual. `Collect perf summary` / `Upload perf artifact` skipped all 34 runs, so **`perf-summary.json` has never existed**. Primary task: (1) derive the import table from `WebAssembly.Module.imports(module)` with warn-once typed stubs, (2) implement the two real symbols, (3) replace the vacuous guard with an `HYPHON_REQUIRE_NATIVE=1` integration assertion + a no-binary synthetic-module test, (4) port the wasm stub to the perf tier **and hoist it into `vitest.shared.ts`**, (5) rewrite the render-count assertions as relationships (#1237 **Part A only** — Part B explicitly out of scope), (6) verify `test:perf` exits 0 **and actually writes `perf-summary.json`**.
Outcome: Planning run. Reconciled: 09-07 Fix First → LANDED & VERIFIED (moved to Done; `main` green on all three workflows; owner closed the whole prior board). `docs/weekly_plan.md` updated (Today's focus = the two dark guards; CI toolchain log 09-14 reconcile; Done 09-14 entry; Backlog: 09-07 P0s closed, new P0-a/P0-b opened, "perf tier is in no pre-merge gate" added, #1234–#1241 wave recorded, `pnpm.overrides` re-verified still dead; Ideas: parked #1138/#1136 marked stale against the new wave with #1240/#1241 flagged as the next non-Fix-First picks; wins table 09-14 row; Last run). Dispatch: kimi-cli swarm = #1236 derived import table + fail-loud guards **and** the perf-tier restoration (scoped to `src/audio-worklets/hyphonNativeImports.ts`, `src/__tests__/hyphonNativeImports.test.ts`, `src/__tests__/SamplerPanel.perf.test.tsx`, `vitest.perf.config.ts`, `vitest.shared.ts`, `src/test/wasmInitStub.ts`, `.github/workflows/ci.yml` + `test-perf.yml`, `docs/wasm/BUILD_NOTES.md` + `docs/PERFORMANCE_BUDGET.md`; **must not touch `src/hooks/useAppState.tsx`, `src/contexts/`, `src/stores/`, `src/App.tsx`, any worklet processor, or `vite.config.ts`**), self-verifying with `.swarm-state.md` checkpoints; decoupled Copilot issue = **#1238** dist budget + `manualChunks` + lazy modals (touches `vite.config.ts`, `scripts/check-release-dist.mjs`, modal components — zero overlap with the WASM-import/perf-tier files); Claude Code = whole-stack build → deploy dry-run → smoke, with the smoke step asserting **native** 303 init rather than a fallback; Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable in this environment (**11th consecutive run**) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions, and `Ideas` still holds no fresh user-written entries.

## Last run (archived) — 2026-09-07
Date: 2026-09-07
Mode: **Fix First** (2nd consecutive)
Focus: **Restore the CI gate, then restore Playwright E2E.** Reconciled first: the 08-31 three-part Fix First **fully LANDED** via `0608199` + `28ec7d2` — P0-a (SessionLaunchEngine accumulator + regression test), P0-b (#1177: `PcfEffect.ts` **and** `masterLoudnessStage.ts` → `?worker&url`, plus a static guard test, an ESLint `no-restricted-syntax` rule, and `dist/` worklet assertions), and P0-c (#1176: export map populated, `check-release-dist.mjs` gate extended, export-name diagnostic logging in Open303/Prophecy) — the last of which `.swarm-state.md` still wrongly reports open. **But `main`'s entire CI surface is dead.** `06ce5b2` ("push fix", a **direct push to `main`**, 09-07 09:44 +0200) added `"packageManager": "pnpm@12.3.4+sha512…"` at `package.json:108` and regenerated `pnpm-lock.yaml` under pnpm 12 (283L; the `lockfileVersion: '9.0'` file now carries pnpm-12-only `configDependencies`/`packageManagerDependencies` keys). All 9 `pnpm/action-setup@v4` call sites across 4 workflow files pin `version: 9`, and v4 hard-errors on `Multiple versions of pnpm specified` — so `CI`, `Playwright E2E`, `303 Quality & Benchmarks` and `Test Performance` each die ~1 s in on every push (run **#1656** / `34099099113`, `Source Integrity (fail-fast)`, 08:09:41Z). Last green CI = run **#1646** on `ab8000b` (07:40Z); **four merges have landed on a dead gate since** (#1208/#1209/#1211/#1212). Separately, **Playwright E2E is red on 12/12 visible `main` runs since ≥09-01**, in two shapes (fast ~90 s vs. full ~90 min with a 541 MB report upload) — cause not established this run. Primary task: (1) drop `version: 9` from all 9 action-setup blocks so the action reads `packageManager` (**not** the reverse — the lockfile is pnpm-12-shaped) — **done in PR #1213, verified locally under pnpm 12.3.4**, (2) guard the collision class in `source-integrity` + `.husky/pre-push`, (3) root-cause and restore E2E, (4) standing owner action, 4th instance — required branch-protection checks on `main`.
Outcome: Planning run. Reconciled: 08-31 P0-a/b/c → LANDED & VERIFIED (moved to Done; owner action noted — #1176/#1177 shipped but still open on GitHub, as are #1139/#1152/#1153). `docs/weekly_plan.md` updated (Today's focus = the 3-part CI/E2E Fix First + the branch-protection ask; CI toolchain log 09-07 reconcile; Ideas: no Idea picked, #1138/#1136 stay parked; Backlog: P0-a/b/c promoted + #1204/#1203/#1181 + shipped-but-open issues + stale-swarm-state note; Done: the 08-31 three-part entry; wins table 08-31 landed + 09-07 pending rows; Last run). Dispatch: **kimi-cli swarm = the 3-part Fix First** (scoped to `.github/workflows/**`, `package.json` `packageManager` only, `pnpm-lock.yaml`, `.husky/`, `scripts/` guards, `playwright.config.ts`, `tests/**`, and `src/` only where an E2E failure proves a real product bug; **must NOT touch** `src/audio-worklets/**`, `src/importers/rbs/**`, `emscripten/**`, or the engine/TTS surfaces); **decoupled Copilot lane (B) = owner issue #1204** — split `src/audio-worklets/open303-processor.ts` (1054L) into modules <700L, expanded body drafted in the dispatch (the filed issue is a thin auto-audit stub); zero overlap with CI config, the E2E harness, or the lockfile. 3 chat-model expansions (Gemini/Kimi/Grok) target #1204; Claude Code = whole-stack build → `dist/` artifact audit → deploy dry-run → smoke, retargeted to **verify the 08-31 #1176/#1177 fixes actually hold on a real production build** (the gates now exist; nobody has run them end-to-end since they landed). Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable in this environment (**10th consecutive run**) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions. `Ideas` still holds no fresh user-written entries; today's Fix First was determined entirely from live CI job logs, git history, and source inspection. **Not established:** the Playwright E2E root cause (deliberately left as work, not guessed).

<details><summary>Archived: 2026-08-31 Last run — FIX FIRST (SessionLaunchEngine + #1176/#1177)</summary>

Date: 2026-08-31
Mode: **Fix First** (streak restarts after one New-Idea run)
Focus: **Restore green `main`, then fix the `/hyphon/` deployed-asset resolution class gutting the audio engine.** Reconciled first: both 08-24 lanes LANDED — #1139 RBS song-mode TRAK export + `handleRbsImport` merged as **#1154** (`33a2b54`; `.swarm-state.md` Checkpoint 1 = all 5 criteria met, 1596 unit tests), and the Copilot lane P0 #1134 shadow-stack-deletion slice merged as **#1155** (`d4b6b59`, protobufjs override verified at `package.json:104`). **But three things are broken.** (1) `main` HEAD does not compile — run **#1581** (`33379973535`, 08-31 09:55, `0f2a5b0`) fails `Source Integrity (fail-fast)` with `SessionLaunchEngine.ts(187,9)`/`(190,19)` **TS2304 Cannot find name 'nextQueued'**: `:181` declares `remaining` while `:187`/`:190` use `nextQueued`. It is also a runtime **`ReferenceError` in `tick()`** whenever a queued launch event isn't yet due, so the Session Launcher is genuinely broken. From ⚡ Bolt **#1174** (`bcabfff`, 08-30) — **merged while its own CI run #1576 was `cancelled`**. Sampled window: 11 failure / 11 success / 8 cancelled. (2) Owner bug **#1177** — `PcfEffect.ts:25` resolves its worklet module as a raw `.ts` `new URL(..., import.meta.url)`, which Vite never emits, so it 404s under `/hyphon/` → `AbortError`, PCF bypassed; the working worklets (`InternalClockAdapter.ts:8`, `singing-voice/workletSetup.ts:1`) use `?worker&url`, and `masterLoudnessStage.ts:21` carries the same raw-`.ts` pattern (likely a silent second casualty, killing #1095's ISP limiter + LUFS metering since 08-17 — verify). (3) Owner bug **#1176** — `hyphon_native.wasm` instantiates on the deployed build but `loadHyphonWasmExportMap()` returns empty, so minified `open303_*`/`prophecy_*` symbols can't be resolved and both engines degrade to JS fallbacks ("explains sequencer/303/prophecy playback feeling dead"). `tools/check_wasm_export_map.mjs` gates the build, not the deployed artifact set.
Outcome: Planning run. Reconciled: 08-24 #1139 + #1134-slice → LANDED & VERIFIED (both moved to Done; owner action noted — issues #1139, #1152, #1153 are still open on GitHub despite the work having shipped). weekly_plan.md updated (Today's focus = the 3-part Fix First + the standing branch-protection action; CI toolchain log 08-31 reconcile; Ideas: #1139 marked done, no Idea picked, 2 directions stay parked; Backlog: P0-a/P0-b/P0-c promoted + owner branch-protection action + #1139 closed out; Done: #1154 + #1155 entries; wins table 08-24 landed + 08-31 pending rows; Last run). Dispatch: **kimi-cli swarm = the 3-part Fix First** (scoped to `src/session/SessionLaunchEngine.ts`, `src/engines/PcfEffect.ts`, `src/audio/loudness/masterLoudnessStage.ts`, `src/engines/Open303Oscillator.ts`/`ProphecyOscillator.ts`, `src/audio-worklets/hyphonNativeImports.ts`, `scripts/check-release-dist.mjs`, `tools/check_wasm_export_map.mjs`, `vite.config.ts` if the base demands it, plus their tests; **must NOT touch** `src/importers/rbs/**`, the WAM2 host, TTS/phoneme code, `emscripten/**` C++ sources, or root-level files); **decoupled Copilot issue (B) = root-file hygiene** — 15 git-tracked junk files at repo root (805 KB `.webm`, 214 KB + 36 KB verification PNGs, 101 KB `test_output.txt`, `fix_components.patch`, `previous_changes.patch`, `build_log.txt`, `lint_output.txt`, `build_pid.txt`, `pr_body.txt`, `verification_notes.txt`, `test_perf.ts`, `git.sh`, `fix_tabindex.sh`, `patch_vitest.cjs`, `svg-demo.html`) plus extending `scripts/check-root.mjs`, which today gates only root `*.py` and `*.md` — zero overlap with the engine/worklet/session files. 3 chat-model expansions (Gemini/Kimi/Grok) target issue B; Claude Code = whole-stack build → `dist/` artifact audit → deploy dry-run → smoke, retargeted this week to **prove or disprove the #1176/#1177 deployed-artifact hypotheses**; Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable in this environment (**9th consecutive run**) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions. `Ideas` still holds no fresh user-written entries; today's Fix First was determined entirely from live CI, source inspection, and the owner's two 08-30 bug reports.

</details>


<details><summary>Archived: 2026-08-24 Last run — NEW IDEA (#1139 RBS song-mode)</summary>

Date: 2026-08-24
Mode: **New Idea** (first non-Fix-First run since 07-27)
Focus: **RBS song-mode fidelity — owner issue #1139.** Verified live: last week's 08-17 `.wasm?init` unit-tier Fix First LANDED & VERIFIED (`src/test/wasmInitStub.ts` + `wasm-stub-resolve` pre-plugin in `vitest.unit.config.ts`; merged #1142/run #1507 `success`) and `main` is GREEN (HEAD `935486e`/#1149 = run #1522 `success`, ~9 trailing green runs; three isolated 08-18/19 single-commit reds fixed-forward behind the green HEAD). Foundation solid → no Fix First. No fresh user-written Ideas (chat tools unavailable, 8th run); the current generated Ideas (#1033/#1035) are superseded by the owner's 08-21 roadmap wave (#1134–#1140) → New Idea mode, curated 3 directions from that roadmap and picked #1139 (RBS song-mode TRAK export + finish `handleRbsImport`/`TODO(#651)` + `ImportReport` + one Playwright path; self-verifiable via export→import round-trip goldens). Parked #1138 (live high-fid 303) and #1136 (AudioContext sampleRate/`setSinkId`) in Ideas.
Outcome: Planning run. Reconciled: 08-17 `.wasm?init` Fix First → LANDED & VERIFIED (moved to Done; owner "require CI checks on `main`" action still carried). weekly_plan.md updated (Today's focus = #1139; CI toolchain log 08-24 reconcile; Ideas: New Idea note + generated entries superseded by owner issues + 2 parked directions; Backlog: #1139 active + owner 08-21 roadmap wave + P0 #1134; Done: `.wasm?init` stub entry; wins table 08-17 landed + 08-24 pending rows; Last run). Dispatch: **kimi-cli swarm = #1139 RBS song-mode fidelity** (scoped to `src/importers/rbs/**`, `src/hooks/useSongStorage.ts`, `src/components/RbsImportModal.tsx` + `rbs-import-modal/**`, `src/utils/rbsImportUtils.ts`, `src/__tests__/Rbs*` + rbs-corpus snapshots, `tests/rbs-automation.spec.ts`, `docs/audio-engine/RBS_IMPORT_PIPELINE.md`, `.swarm-state.md`; **must NOT touch** the real-time worklet, `src/hooks/audioEngine/**` playback, `src/audio/playback/**`, `emscripten/**`, WASM, `eslint.config.js`, `.github/workflows/**`, or the shadow-stack files); **decoupled Copilot issue (B) = shadow-playback-stack deletion slice of owner P0 #1134** (`hooks/audioEngine/samplerPlayback.ts` [973L unused] + `audio/playback/{synth,drum,sampler}Playback.ts` + `index.ts` [unused] + import-graph guard test + protobufjs `>=7.5.8` override — zero overlap with the RBS files; satisfies the owner's "delete shadow stacks before feature epics" gate). 3 chat-model expansions (Gemini/Kimi/Grok) target issue B; Claude Code = whole-stack build → deploy dry-run → smoke (audio init + WebGPU knob render); Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable this environment (8th consecutive run) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions. `Ideas` holds no fresh user-written entries, so New-Idea direction curation leaned on the owner's 08-21 GitHub roadmap wave (the strongest available in-context signal).

</details>

<details><summary>Archived: 2026-08-17 Last run — FIX FIRST (`.wasm?init` unit tier)</summary>

Date: 2026-08-17
Mode: **Fix First**
Focus: Restore green `main` — the 08-10 #1075 CI-integrity tiering LANDED (fail-fast `source-integrity` `tsc` job + `lint`/`test-unit`/`test-integration` tiers; Copilot lane #1095 master ISP limiter + LUFS also shipped), but the tiered restructure introduced a **new** break: the no-WASM `test-unit` job fails at Vite `import-analysis` resolving static `.wasm?init` imports. `main` red across the whole sampled window (23 failure / 3 success / 4 cancelled, 08-14→08-17, HEAD `55b03690`; run `32025223825` job "Unit tests (Vitest)": `Failed to resolve import "../wasm/fft.wasm?init"`). The 4 top-level `.wasm?init` importers (`fftLoader.ts`/`WasmOscillator.ts`/`audioExport.ts`/`xmExport.ts`) can't resolve because `src/wasm/*.wasm` is gitignored + not built in the unit job; `source-integrity`'s `tsc` gate is blind (ambient `declare module '*.wasm'`). Primary task: hermetic `*.wasm?init` stub alias in `vitest.unit.config.ts` (all 4 importers + future ones resolve without binaries), drive `test:unit`+`test:integration`+`lint`+`tsc -b` clean, teach the fast lane to catch unresolved bundler imports, and require CI on `main` (branch protection — owner action).
Outcome: Planning run. Reconciled: 08-10 #1075 CI-integrity Fix First → LANDED (moved to Done; owner "require checks on main" action carried); 08-10 Copilot lane master limiter/LUFS #1095 → LANDED (Idea retired). weekly_plan.md updated (Today's focus = `.wasm?init` unit-tier fix; CI toolchain log 08-17 reconcile; Backlog: #1075 done + new break promoted to P0; Ideas: master-limiter idea retired + note on fresh owner issues #1105/#1106; Done: #1075 + #1095 entries; wins table 08-10 landed + 08-17 pending rows; Last run). Dispatch: kimi-cli swarm = fix the no-WASM unit-tier `.wasm?init` resolution + drive both test tiers to clean exit (scoped to `vitest.unit.config.ts`, `vitest.setup.unit.ts`, a new `src/test/` stub, `.github/workflows/ci.yml`, and the 2 failing specs; **must NOT touch** `src/audio/graph/`, `src/components/Knob*`/`MagicKnob.tsx`/`KnobGPUContext.ts`/`knobMaterial.ts`, or the 4 loader source files unless a stub-only fix is impossible); decoupled Copilot issue = **owner #1106 WebGPU hard-fail probe** (retargeted 08-17: the originally-drafted knob reduced-motion issue was discarded after verifying it already landed on `main` — see Done; a shared `probeWebGPU()` used by `WebGpuBackend` init + HUD hard-fail is genuinely unbuilt, scoped to `src/engines/backends/BackendRegistry.ts` + `src/services/WebGpuBackend.ts` + EngineHUD, zero overlap with kimi-cli's test-infra or the audio hot path); 3 chat-model expansions (Gemini/Kimi/Grok) targeted #1106; Claude Code = whole-stack build → deploy dry-run → smoke (audio init + WebGPU knob render); Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable this environment (8th consecutive run) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions. `Ideas` still holds no fresh user-written entries, but owner filed GitHub issues #1105/#1106 this week (closest in-context signal).

</details>

<details><summary>Archived: 2026-08-10 Last run — FIX FIRST (#1075)</summary>

Date: 2026-08-10
Mode: **Fix First**
Focus: Execute owner P0 **#1075** — restore parse/build/test integrity on `main` + make CI fail-fast and enforced. Verified live: last week's 08-03 E2E Fix First LANDED (matrix green, `.orig` purged, root `weekly_plan.md` removed — #1046/#1052). But `main` CI red on the majority of the week's commits (14 failure / 11 success in the sampled `ci.yml` window; 08-05→08-08 near-continuous red), owner filed P0 #1075 (2026-08-08, "fix before feature work"), and a fresh `tsc` error red-ed `8707bc3`/#1071 today (`SynthModulationEffects.tsx` `currentFormantShift` possibly undefined). HEAD `e04a8ee` green but the class recurs because CI has no fail-fast parser job and isn't a required check on `main`. The 07-27 New-Idea focus (Phase-1 TB-303 #974) is paused — foundation first.
Outcome: Planning run. Reconciled: 08-03 E2E Fix First → LANDED (moved to Done; #1036/#1039 closed out). weekly_plan.md updated (Today's focus = #1075; CI toolchain log 08-10 reconcile; Done 08-03 E2E entry; Backlog: #1075 promoted to P0 focus, #1036/#1039 done, mega-module splits deferred; Ideas: Fix First note, 3 generated directions stay parked; wins table 08-10 row; Last run). Dispatch: kimi-cli swarm = execute #1075 (scoped to the flagged src files + `ci.yml` + `.husky/`; must NOT touch `src/audio/graph/` or new metering files); decoupled Copilot issue = master true-peak (ISP) limiter + LUFS/peak metering suite on `src/audio/graph/` (new limiter node + new meter worklet + new `MasterMeter` panel — zero overlap with #1075's files); 3 chat-model expansions (Gemini/Kimi/Grok) on the metering issue; Claude Code = whole-stack build → deploy dry-run → smoke (audio init + WebGPU knob render); Jules wrap-up template + 2 Gemini review prompts provided. Noah notified via push. **Context gap:** chat-history tools (`recent_chats`/`conversation_search`) unavailable this environment (7th consecutive run) — context from repo state + GitHub MCP + plan file only; no visibility into Noah's in-week chat mentions, and `Ideas` still holds no fresh user entries.

</details>

## Last run (archived) — 2026-08-03
Date: 2026-08-03 (first run — no prior weekly_plan.md existed)
Mode: Fix First
Focus: Restore green Playwright E2E (#1036) + purge `.orig` merge artifacts (#1039)
Outcome: Plan generated. Dispatch produced for kimi-cli (E2E fix), Copilot issue (master limiter/LUFS lane), 3 chat-model expansions, Claude Code whole-stack pipeline exercise, Jules wrap-up template, and 2 Gemini review prompts. weekly_plan.md created and committed.
