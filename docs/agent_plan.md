## Active Backlog
- [x] Implement Phoneme-Aware Velocity scaling for SingingVoice.
- [x] Implement TTS playback reversing per step.

## Innovation Lab
- What if we could reverse the TTS sample per step?
- What if phoneme categories scaled differently based on MIDI velocity?
- What if we add pitch envelopes that trigger on velocity thresholds?

## Refactor
- Consider adding a global cross-fade smoothing when `reverse` toggle switches midway through playback.
