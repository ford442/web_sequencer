Custom Sample Slicing UI is implemented.

`WaveformDisplay.tsx` supports manual add/move/remove of slice markers via mouse and keyboard, and `SamplerPanel.tsx` wires `onAlignmentChange` to `audioEngine.updateAlignment`.
