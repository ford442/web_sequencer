--- src/engines/SingingVoiceManager.ts
+++ src/engines/SingingVoiceManager.ts
@@ -156,6 +156,9 @@
     }

     stopAll() {
+        for (const [index, activeVoice] of this.activeVoices.entries()) {
+            activeVoice.voice.noteOff(this.audioContext.currentTime);
+        }
         this.activeVoices.clear();
         // Reset params on all voices?
     }
